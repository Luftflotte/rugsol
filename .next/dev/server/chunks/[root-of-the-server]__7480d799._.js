module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/punycode [external] (punycode, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("punycode", () => require("punycode"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/tls [external] (tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tls", () => require("tls"));

module.exports = mod;
}),
"[externals]/node:url [external] (node:url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:url", () => require("node:url"));

module.exports = mod;
}),
"[project]/lib/solana/connection.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "connection",
    ()=>connection,
    "fetchTokenContext",
    ()=>fetchTokenContext,
    "getTokenMintInfo",
    ()=>getTokenMintInfo
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
;
const HELIUS_API_KEY = process.env.HELIUS_API_KEY;
if (!HELIUS_API_KEY) {
    console.warn("HELIUS_API_KEY is not set in environment variables");
}
const HELIUS_RPC_URL = `https://mainnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}`;
const connection = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Connection"](HELIUS_RPC_URL, "confirmed");
async function getTokenMintInfo(address) {
    try {
        const mintPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](address);
        const accountInfo = await connection.getParsedAccountInfo(mintPublicKey);
        if (!accountInfo.value) {
            return null;
        }
        const data = accountInfo.value.data;
        if (!("parsed" in data)) {
            return null;
        }
        const parsedInfo = data.parsed.info;
        return {
            mintAuthority: parsedInfo.mintAuthority ?? null,
            freezeAuthority: parsedInfo.freezeAuthority ?? null,
            supply: parsedInfo.supply,
            decimals: parsedInfo.decimals
        };
    } catch (error) {
        console.error("Error fetching token mint info:", error);
        return null;
    }
}
async function fetchTokenContext(address) {
    try {
        const mintPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](address);
        const accountInfo = await connection.getParsedAccountInfo(mintPublicKey);
        if (!accountInfo.value || !("parsed" in accountInfo.value.data)) {
            return null;
        }
        const info = accountInfo.value.data.parsed.info;
        return {
            mintAuthority: info.mintAuthority ?? null,
            freezeAuthority: info.freezeAuthority ?? null,
            supply: BigInt(info.supply),
            decimals: info.decimals
        };
    } catch (error) {
        console.error("Error fetching token context:", error);
        return null;
    }
}
}),
"[project]/lib/external/price.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "fetchJupiterPrice",
    ()=>fetchJupiterPrice,
    "getTokenPrice",
    ()=>getTokenPrice
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
;
async function calculateMarketCap(mintAddress, priceUsd) {
    // Special case for native SOL supply (~589M)
    if (mintAddress === "So11111111111111111111111111111111111111111" || mintAddress === "So11111111111111111111111111111111111111112") {
        return priceUsd * 589000000;
    }
    try {
        const mintInfo = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getTokenMintInfo"])(mintAddress);
        if (!mintInfo) return 0;
        const supply = Number(mintInfo.supply);
        const adjustedSupply = supply / Math.pow(10, mintInfo.decimals);
        return priceUsd * adjustedSupply;
    } catch (e) {
        console.warn("Failed to calculate MC manually:", e);
        return 0;
    }
}
async function getTokenPrice(mintAddress) {
    // Hard override for major stablecoins to prevent "price glitch" from low-liquidity pairs
    const STABLECOINS = {
        "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v": "USDC",
        "Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB": "USDT"
    };
    // Hard override for SOL/wSOL — Jupiter Price API returns 404 for these
    const SOL_ADDRESSES = [
        "So11111111111111111111111111111111111111111",
        "So11111111111111111111111111111111111111112"
    ];
    if (SOL_ADDRESSES.includes(mintAddress)) {
        // Fetch SOL price from DexScreener (wSOL pairs) or CoinGecko as fallback
        try {
            const response = await fetch("https://api.coingecko.com/api/v3/simple/price?ids=solana&vs_currencies=usd&include_24hr_change=true");
            if (response.ok) {
                const data = await response.json();
                const priceUsd = data.solana?.usd || 0;
                const h24Change = data.solana?.usd_24h_change || 0;
                if (priceUsd > 0) {
                    const marketCap = priceUsd * 589_000_000;
                    return {
                        priceUsd,
                        marketCap,
                        liquidity: {
                            usd: 1_000_000_000,
                            base: 0,
                            quote: 0
                        },
                        priceChange: {
                            m5: 0,
                            h1: 0,
                            h6: 0,
                            h24: parseFloat(h24Change.toFixed(2))
                        },
                        pairCreatedAt: 0
                    };
                }
            }
        } catch (e) {
            console.warn("CoinGecko SOL price fetch failed:", e);
        }
        // Absolute fallback — return null so the caller handles it
        return null;
    }
    if (STABLECOINS[mintAddress]) {
        const priceUsd = 1.00;
        const marketCap = await calculateMarketCap(mintAddress, priceUsd);
        return {
            priceUsd,
            marketCap,
            liquidity: {
                usd: 1000000000,
                base: 0,
                quote: 0
            },
            priceChange: {
                m5: 0,
                h1: 0,
                h6: 0,
                h24: 0
            },
            pairCreatedAt: 0
        };
    }
    // Map native SOL address (...1) to Wrapped SOL (...2) for price APIs
    const effectiveAddress = mintAddress === "So11111111111111111111111111111111111111111" ? "So11111111111111111111111111111111111111112" : mintAddress;
    // Try Jupiter first (most reliable for Solana)
    const jupPrice = await fetchJupiterPrice(effectiveAddress);
    if (jupPrice && jupPrice.priceUsd > 0) {
        try {
            const response = await fetch(`https://api.dexscreener.com/latest/dex/tokens/${effectiveAddress}`);
            if (response.ok) {
                const data = await response.json();
                const bestPair = data.pairs?.sort((a, b)=>(b.liquidity?.usd || 0) - (a.liquidity?.usd || 0))[0];
                if (bestPair) {
                    return {
                        ...jupPrice,
                        marketCap: bestPair.marketCap || bestPair.fdv || jupPrice.marketCap,
                        liquidity: {
                            usd: bestPair.liquidity?.usd || 0,
                            base: bestPair.liquidity?.base || 0,
                            quote: bestPair.liquidity?.quote || 0
                        },
                        priceChange: bestPair.priceChange || jupPrice.priceChange,
                        pairCreatedAt: bestPair.pairCreatedAt
                    };
                }
            }
        } catch (e) {}
        return jupPrice;
    }
    // Fallback to DexScreener
    try {
        const response = await fetch(`https://api.dexscreener.com/latest/dex/tokens/${effectiveAddress}`);
        if (!response.ok) return null;
        const data = await response.json();
        if (!data.pairs || data.pairs.length === 0) return null;
        const bestPair = data.pairs.sort((a, b)=>(b.liquidity?.usd || 0) - (a.liquidity?.usd || 0))[0];
        // DexScreener token endpoint usually returns the price of the searched token directly
        const priceUsd = parseFloat(bestPair.priceUsd);
        let marketCap = bestPair.fdv || bestPair.marketCap || 0;
        if (marketCap === 0) marketCap = await calculateMarketCap(effectiveAddress, priceUsd);
        return {
            priceUsd,
            marketCap,
            liquidity: {
                usd: bestPair.liquidity?.usd || 0,
                base: bestPair.liquidity?.base || 0,
                quote: bestPair.liquidity?.quote || 0
            },
            priceChange: bestPair.priceChange || {
                m5: 0,
                h1: 0,
                h6: 0,
                h24: 0
            },
            pairCreatedAt: bestPair.pairCreatedAt
        };
    } catch (error) {
        return null;
    }
}
async function fetchJupiterPrice(mintAddress) {
    try {
        const headers = {
            "Accept": "application/json"
        };
        if (process.env.JUPITER_API_KEY) {
            headers["x-api-key"] = process.env.JUPITER_API_KEY;
        }
        const response = await fetch(`https://api.jup.ag/price/v2?ids=${mintAddress}`, {
            headers
        });
        if (!response.ok) {
            console.warn(`Jupiter Price API returned ${response.status}`);
            return null;
        }
        const json = await response.json();
        const data = json.data?.[mintAddress];
        if (!data || !data.price) return null;
        const priceUsd = parseFloat(data.price);
        // Jupiter Price API v2 doesn't return MC, so we MUST calculate it
        const marketCap = await calculateMarketCap(mintAddress, priceUsd);
        return {
            priceUsd: priceUsd,
            marketCap: marketCap,
            liquidity: {
                usd: 0,
                base: 0,
                quote: 0
            },
            priceChange: {
                m5: 0,
                h1: 0,
                h6: 0,
                h24: 0
            },
            pairCreatedAt: 0
        };
    } catch (error) {
        console.warn("Failed to fetch price from Jupiter:", error);
        return null;
    }
}
}),
"[project]/lib/external/jupiter-swap.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "checkSellSimulation",
    ()=>checkSellSimulation
]);
const SOL_MINT = "So11111111111111111111111111111111111111112";
async function checkSellSimulation(tokenAddress) {
    // Don't check SOL itself
    if (tokenAddress === SOL_MINT) {
        return {
            isHoneypot: false,
            simulationSuccess: true
        };
    }
    try {
        // Try to swap a small amount of Token -> SOL
        // We assume 6 decimals as standard for memecoins, so 1000000 = 1 token.
        // Even if 9 decimals, 1000000 = 0.001 token, which is usually tradable.
        const amount = "1000000";
        const url = `https://quote-api.jup.ag/v6/quote?inputMint=${tokenAddress}&outputMint=${SOL_MINT}&amount=${amount}&slippageBps=500`; // 5% slippage allowance
        const response = await fetch(url);
        if (!response.ok) {
            const errorText = await response.text();
            // If 400 Bad Request, usually means "No route found" or "Insufficient liquidity"
            if (response.status === 400 || response.status === 404) {
                return {
                    isHoneypot: true,
                    simulationSuccess: false,
                    reason: "Jupiter could not find a swap route (No liquidity or unsellable)"
                };
            }
            // Other errors (500) might be Jupiter issues, don't flag as honeypot strictly
            return {
                isHoneypot: false,
                simulationSuccess: false,
                reason: "Jupiter API error"
            };
        }
        const data = await response.json();
        if (!data.outAmount || data.outAmount === "0") {
            return {
                isHoneypot: true,
                simulationSuccess: false,
                reason: "Swap simulation returned 0 SOL output"
            };
        }
        // Success!
        return {
            isHoneypot: false,
            simulationSuccess: true
        };
    } catch (error) {
        // console.warn("Honeypot check failed (network/API error):", error instanceof Error ? error.message : String(error));
        return {
            isHoneypot: false,
            simulationSuccess: false,
            reason: "Check failed (Network/API Error)"
        };
    }
}
}),
"[project]/lib/solana/authority.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "checkFreezeAuthority",
    ()=>checkFreezeAuthority,
    "checkMintAuthority",
    ()=>checkMintAuthority
]);
function checkMintAuthority(context) {
    if (context.mintAuthority === null) {
        return {
            status: "pass",
            value: "Revoked"
        };
    }
    return {
        status: "fail",
        value: context.mintAuthority
    };
}
function checkFreezeAuthority(context) {
    if (context.freezeAuthority === null) {
        return {
            status: "pass",
            value: "Revoked"
        };
    }
    return {
        status: "fail",
        value: context.freezeAuthority
    };
}
}),
"[project]/lib/solana/holders.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "analyzeHolders",
    ()=>analyzeHolders
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
;
;
const HELIUS_API_KEY = process.env.HELIUS_API_KEY;
const HELIUS_RPC_URL = `https://mainnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}`;
// LP Pool program addresses to filter out
const LP_PROGRAM_ADDRESSES = new Set([
    "675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8",
    "whirLbMiicVdio4qvUfM5KAg6Ct8VwpYzGff3uctyCc"
]);
/**
 * Fallback: Use Helius DAS API to get token accounts when getTokenLargestAccounts
 * fails for tokens with too many holders (e.g. USDC with 21M+ accounts).
 */ async function fetchLargestAccountsViaDAS(mintAddress) {
    const response = await fetch(HELIUS_RPC_URL, {
        method: "POST",
        headers: {
            "Content-Type": "application/json"
        },
        body: JSON.stringify({
            jsonrpc: "2.0",
            id: 1,
            method: "getTokenAccounts",
            params: {
                mint: mintAddress,
                limit: 20,
                options: {
                    showZeroBalance: false
                }
            }
        })
    });
    const data = await response.json();
    if (!data.result?.token_accounts?.length) {
        return null;
    }
    // Sort by amount descending to get largest holders
    const sorted = data.result.token_accounts.sort((a, b)=>Number(BigInt(b.amount) - BigInt(a.amount)));
    return sorted.map((acc)=>({
            address: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](acc.address),
            amount: acc.amount,
            owner: acc.owner
        }));
}
async function analyzeHolders(mintAddress, totalSupply, excludeOwners = []) {
    try {
        const mintPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        if (totalSupply === BigInt(0)) {
            return {
                topHolders: [],
                topTenPercent: 0,
                largestHolder: null,
                clustersDetected: 0
            };
        }
        // Get top 20 largest accounts
        let largestAccounts;
        let usedDASFallback = false;
        try {
            // getTokenLargestAccounts only works for valid SPL Token mints
            largestAccounts = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getTokenLargestAccounts(mintPublicKey);
            if (!largestAccounts || !largestAccounts.value) {
                throw new Error("No holder data returned from RPC");
            }
        } catch (rpcError) {
            const msg = rpcError.message || "";
            // For tokens with too many accounts, fall back to Helius DAS API
            if (msg.includes("Too many accounts") || msg.includes("number of accounts")) {
                console.warn(`Too many holders for ${mintAddress}, falling back to Helius DAS API`);
                try {
                    const dasAccounts = await fetchLargestAccountsViaDAS(mintAddress);
                    if (dasAccounts && dasAccounts.length > 0) {
                        largestAccounts = {
                            value: dasAccounts
                        };
                        usedDASFallback = true;
                    } else {
                        return {
                            topHolders: [],
                            topTenPercent: 0,
                            largestHolder: null,
                            clustersDetected: 0
                        };
                    }
                } catch (dasError) {
                    console.warn(`DAS fallback also failed for ${mintAddress}:`, dasError);
                    return {
                        topHolders: [],
                        topTenPercent: 0,
                        largestHolder: null,
                        clustersDetected: 0
                    };
                }
            } else {
                console.warn(`RPC error fetching holders for ${mintAddress}:`, msg || rpcError);
                return {
                    topHolders: [],
                    topTenPercent: 0,
                    largestHolder: null,
                    clustersDetected: 0
                };
            }
        }
        const topHolders = [];
        const excludeSet = new Set(excludeOwners);
        if (usedDASFallback) {
            // DAS response already includes owner info, no need for extra RPC calls
            const accountsToProcess = largestAccounts.value.slice(0, 15);
            for (const account of accountsToProcess){
                const owner = account.owner;
                const amount = account.amount;
                const addressStr = typeof account.address === "string" ? account.address : account.address.toBase58();
                const percent = Number(BigInt(amount) * BigInt(10000) / totalSupply) / 100;
                const isLpPool = LP_PROGRAM_ADDRESSES.has(owner) || excludeSet.has(owner) || excludeSet.has(addressStr);
                topHolders.push({
                    address: addressStr,
                    owner,
                    amount,
                    percent,
                    isLpPool
                });
            }
        } else {
            // Standard path: need to fetch owner info for each account
            const accountsToProcess = largestAccounts.value.slice(0, 15);
            for (const account of accountsToProcess){
                try {
                    const accountInfo = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getParsedAccountInfo(account.address);
                    if (!accountInfo.value || !("parsed" in accountInfo.value.data)) {
                        continue;
                    }
                    const info = accountInfo.value.data.parsed.info;
                    const owner = info.owner;
                    const amount = account.amount;
                    const percent = Number(BigInt(amount) * BigInt(10000) / totalSupply) / 100;
                    // Mark as LP Pool if it's in our known list OR if it's an excluded address (like Pump Curve)
                    const isLpPool = LP_PROGRAM_ADDRESSES.has(owner) || excludeSet.has(owner) || excludeSet.has(account.address.toBase58());
                    topHolders.push({
                        address: account.address.toBase58(),
                        owner,
                        amount,
                        percent,
                        isLpPool
                    });
                } catch (e) {
                    console.warn(`Failed to fetch info for account ${account.address.toBase58()}:`, e);
                    continue;
                }
            }
        }
        // For calculations, we exclude LP pools
        const nonLpHolders = topHolders.filter((h)=>!h.isLpPool);
        // Advanced Cluster Detection: Look for "tight groups"
        // Scammers use scripts to distribute supply across 10-50 wallets with slightly varied balances.
        let clustersDetected = 0;
        const holdersForAnalysis = nonLpHolders.slice(0, 20);
        // Group holders into buckets of 0.1% width
        const buckets = new Map();
        holdersForAnalysis.forEach((h)=>{
            if (h.percent < 0.3) return; // Ignore small fish
            const bucketId = Math.floor(h.percent * 10); // 0.1% steps
            buckets.set(bucketId, (buckets.get(bucketId) || 0) + 1);
        });
        for (const count of buckets.values()){
            if (count >= 5) {
                clustersDetected += count;
            }
        }
        // Also check for "Linear Distribution" (e.g. 0.99, 0.98, 0.97...)
        // If top 10 holders are all within a 0.5% total range
        if (holdersForAnalysis.length >= 10) {
            const p1 = holdersForAnalysis[0].percent;
            const p10 = holdersForAnalysis[9].percent;
            if (p1 - p10 < 0.2 && p1 > 0.5) {
                // If the difference between 1st and 10th holder is less than 0.2%
                // and they aren't just tiny wallets, this is a massive cluster.
                clustersDetected = Math.max(clustersDetected, 10);
            }
        }
        // Calculate top 10 percentage (excluding LP pools)
        const topTenPercent = nonLpHolders.slice(0, 10).reduce((sum, holder)=>sum + holder.percent, 0);
        // Find largest non-LP holder
        const largestHolder = nonLpHolders.length > 0 ? {
            address: nonLpHolders[0].owner,
            percent: nonLpHolders[0].percent
        } : null;
        return {
            topHolders,
            topTenPercent: Math.round(topTenPercent * 100) / 100,
            largestHolder,
            clustersDetected
        };
    } catch (error) {
        console.error("Error analyzing holders:", error);
        return null;
    }
}
}),
"[project]/lib/external/liquidity.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "checkLiquidity",
    ()=>checkLiquidity,
    "fetchDexScreener",
    ()=>fetchDexScreener
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$price$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/price.ts [app-route] (ecmascript)");
;
;
;
const BIRDEYE_API_KEY = process.env.BIRDEYE_API_KEY;
// Burn addresses for LP tokens
const BURN_ADDRESSES = new Set([
    "1111111111111111111111111111111111",
    "1nc1nerator11111111111111111111111111111111"
]);
async function fetchDexScreener(mintAddress) {
    try {
        const response = await fetch(`https://api.dexscreener.com/latest/dex/tokens/${mintAddress}`, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) {
            console.warn(`DexScreener fetch failed for ${mintAddress}: ${response.status}`);
            return null;
        }
        const data = await response.json();
        if (!data.pairs || data.pairs.length === 0) {
            return null;
        }
        // Find Solana pairs and get highest liquidity one
        const solanaPairs = data.pairs.filter((p)=>p.chainId === "solana");
        if (solanaPairs.length === 0) {
            return null;
        }
        // Sort by liquidity USD descending
        solanaPairs.sort((a, b)=>(b.liquidity?.usd || 0) - (a.liquidity?.usd || 0));
        const topPair = solanaPairs[0];
        // Normalize DEX name
        let dexName = topPair.dexId;
        if (dexName === "pump" || dexName === "pumpswap") {
            dexName = "Pump.fun";
        } else if (dexName === "raydium") {
            dexName = "Raydium";
        } else if (dexName === "orca") {
            dexName = "Orca";
        } else if (dexName === "meteora") {
            dexName = "Meteora";
        }
        // Use price from the pair if available, explicitly handle string/number conversion if needed (API usually returns string)
        const priceUsd = Number(topPair.priceUsd || 0);
        return {
            lpSizeUsd: topPair.liquidity?.usd || 0,
            dexName,
            dexId: topPair.dexId,
            pairAddress: topPair.pairAddress,
            name: topPair.baseToken.name,
            symbol: topPair.baseToken.symbol,
            image: topPair.info?.imageUrl,
            priceUsd
        };
    } catch (error) {
        console.error("Error fetching from DexScreener:", error);
        return null;
    }
}
// Check if LP tokens are burned by checking holder distribution
async function checkLpBurned(pairAddress) {
    try {
        const pairPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](pairAddress);
        // Check if it's a valid token mint account first
        const accountInfo = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getParsedAccountInfo(pairPublicKey);
        if (!accountInfo.value || !("parsed" in accountInfo.value.data) || accountInfo.value.data.parsed.type !== "mint") {
            // Not a token mint (likely a pool address), skip holder check to avoid RPC error
            return false;
        }
        const largestAccounts = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getTokenLargestAccounts(pairPublicKey);
        for (const account of largestAccounts.value){
            const accountInfo = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getParsedAccountInfo(account.address);
            if (!accountInfo.value || !("parsed" in accountInfo.value.data)) {
                continue;
            }
            const owner = accountInfo.value.data.parsed.info.owner;
            // Check if owner is burn address
            if (BURN_ADDRESSES.has(owner)) {
                // Check if this burn address holds significant portion (>90%)
                const totalSupply = await getLpTotalSupply(pairAddress);
                if (BigInt(totalSupply) > BigInt(0)) {
                    const burnedAmount = BigInt(account.amount);
                    const burnedPercent = Number(burnedAmount * BigInt(100) / BigInt(totalSupply));
                    if (burnedPercent > 90) {
                        return true;
                    }
                }
            }
        }
        return false;
    } catch (error) {
        console.error("Error checking LP burned status:", error);
        return false;
    }
}
async function getLpTotalSupply(pairAddress) {
    try {
        const pairPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](pairAddress);
        const mintInfo = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getParsedAccountInfo(pairPublicKey);
        if (!mintInfo.value || !("parsed" in mintInfo.value.data)) {
            return "0";
        }
        return mintInfo.value.data.parsed.info.supply || "0";
    } catch  {
        return "0";
    }
}
// Fetch security info from Birdeye (requires API key)
async function fetchBirdeyeSecurity(mintAddress) {
    if (!BIRDEYE_API_KEY) {
        return null;
    }
    try {
        const response = await fetch(`https://public-api.birdeye.so/defi/token_security?address=${mintAddress}`, {
            headers: {
                "X-API-KEY": BIRDEYE_API_KEY,
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) {
            return null;
        }
        const data = await response.json();
        return data.success ? data.data || null : null;
    } catch (error) {
        console.error("Error fetching from Birdeye:", error);
        return null;
    }
}
// Fetch liquidity from Birdeye
async function fetchBirdeyeLiquidity(mintAddress) {
    if (!BIRDEYE_API_KEY) return 0;
    try {
        const response = await fetch(`https://public-api.birdeye.so/defi/v3/token/market-data?address=${mintAddress}`, {
            headers: {
                "X-API-KEY": BIRDEYE_API_KEY,
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) return 0;
        const json = await response.json();
        return json.success ? json.data?.liquidity || 0 : 0;
    } catch (error) {
        console.error("Error fetching Birdeye liquidity:", error);
        return 0;
    }
}
// Fallback: Check Pump.fun API directly
async function fetchPumpFunMarketCap(mintAddress) {
    try {
        const response = await fetch(`https://frontend-api.pump.fun/coins/${mintAddress}`, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) return 0;
        const data = await response.json();
        return data?.market_cap || 0;
    } catch  {
        return 0;
    }
}
// Check if token has graduated from Pump.fun to DEX and fetch DEX liquidity
async function checkGraduatedPumpToken(mintAddress) {
    try {
        // Retry DexScreener with exponential backoff for newly graduated tokens
        const response = await fetch(`https://api.dexscreener.com/latest/dex/tokens/${mintAddress}`, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) return null;
        const data = await response.json();
        if (!data.pairs || data.pairs.length === 0) return null;
        // Find ALL Solana pairs (not just Pump.fun)
        const solanaPairs = data.pairs.filter((p)=>p.chainId === "solana");
        if (solanaPairs.length === 0) return null;
        // Sort by liquidity USD descending and pick the highest
        solanaPairs.sort((a, b)=>(b.liquidity?.usd || 0) - (a.liquidity?.usd || 0));
        const topPair = solanaPairs[0];
        // Normalize DEX name
        let dexName = topPair.dexId;
        if (dexName === "pump" || dexName === "pumpswap") {
            dexName = "Pump.fun";
        } else if (dexName === "raydium") {
            dexName = "Raydium";
        } else if (dexName === "orca") {
            dexName = "Orca";
        } else if (dexName === "meteora") {
            dexName = "Meteora";
        }
        const lpSize = topPair.liquidity?.usd || 0;
        // Only return if we found significant liquidity or DEX is not Pump.fun
        if (lpSize > 0 || dexName && dexName !== "Pump.fun") {
            console.log(`[Liquidity] Graduated token found on ${dexName}: $${lpSize}`);
            return {
                lpSizeUsd: lpSize,
                dexName,
                dexId: topPair.dexId,
                pairAddress: topPair.pairAddress
            };
        }
        return null;
    } catch (error) {
        console.error("Error checking graduated Pump token:", error);
        return null;
    }
}
// Fallback: Check Jupiter Token Search API for liquidity
async function fetchJupiterLiquidity(mintAddress) {
    if (!process.env.JUPITER_API_KEY) return 0;
    try {
        const response = await fetch(`https://api.jup.ag/tokens/v2/search?query=${mintAddress}`, {
            headers: {
                "x-api-key": process.env.JUPITER_API_KEY,
                "Accept": "application/json"
            }
        });
        if (!response.ok) return 0;
        const data = await response.json();
        // The API returns an array of tokens matching the query.
        // We expect the first match to be our token if address was used as query.
        if (Array.isArray(data) && data.length > 0) {
            // Find the exact match just in case
            const token = data.find((t)=>t.address === mintAddress) || data[0];
            return token.liquidity || 0; // "daily_volume" or "liquidity" - user said "liquidity"
        }
        return 0;
    } catch (error) {
        console.error("Error fetching Jupiter liquidity:", error);
        return 0;
    }
}
// Fallback: Check Raydium AMM via RPC (Helius)
// This uses pure RPC calls to find if a pool exists, bypassing API limits.
async function findRaydiumPoolViaRPC(mintAddress) {
    try {
        const mint = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        // Strategy: Check if any of the top 20 holders are DEX vaults
        // This is much lighter on RPC than getProgramAccounts
        const largestAccounts = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getTokenLargestAccounts(mint);
        if (!largestAccounts.value || largestAccounts.value.length === 0) return false;
        // Known DEX Program IDs / Authorities that hold tokens
        const KNOWN_DEX_OWNERS = new Set([
            "5Q544fKrFoe6tsEbD7S8EmxGTJYAKtTVhAW5Q5pge4j1",
            "675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8",
            "CPMMOO8L3FXFWJLTR2E2j15fXW4g1peYjF8zL3fE53H",
            "LBUZKhRxPF3XUpBCjp4YzTKgLccjZhTSDM9YuVaPwxo",
            "whirLbMiicVdio4qvUfM5KAg6Ct8VwpYzGff3uctyCc",
            "9W959DqEETiGZocYWCQPaJ6sBmUzgfxXfqGeTEdp3aQP",
            "CAMMCzo5YL8w4VFF8ESWg8KS2uBlAMFHKq4zzMoERHvi"
        ]);
        for (const account of largestAccounts.value){
            // We need to fetch the account info to see the owner
            const info = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getParsedAccountInfo(account.address);
            if (info.value && "parsed" in info.value.data) {
                const owner = info.value.data.parsed.info.owner;
                if (KNOWN_DEX_OWNERS.has(owner)) {
                    console.log(`[Liquidity] Found DEX holder: ${owner}`);
                    return true;
                }
            }
        }
        return false;
    } catch (e) {
        console.error("RPC Raydium check failed:", e);
        return false;
    }
}
async function checkLiquidity(mintAddress, prefetchedDexData) {
    try {
        let liquidityUsd = 0;
        let dexName = null;
        let pairAddress = null;
        let rawDexId = null;
        // Step 1: Try DexScreener
        const dexData = prefetchedDexData ?? await fetchDexScreener(mintAddress);
        if (dexData) {
            liquidityUsd = dexData.lpSizeUsd;
            dexName = dexData.dexName;
            pairAddress = dexData.pairAddress;
            rawDexId = dexData.dexId || null;
            // Sanity Check 1: If DexScreener has price but 0 liquidity, flag as Unknown (-1)
            if (liquidityUsd === 0 && dexData.priceUsd > 0) {
                console.log(`[Liquidity] DexScreener has price ($${dexData.priceUsd}) but 0 liquidity. Marking as Unknown.`);
                liquidityUsd = -1;
            }
        } else {
            console.log(`[Liquidity] DexScreener returned null for ${mintAddress}`);
        }
        // Step 2: Fallback to Birdeye if Liquidity is 0
        if (liquidityUsd === 0) {
            const birdeyeLiq = await fetchBirdeyeLiquidity(mintAddress);
            if (birdeyeLiq > 0) {
                liquidityUsd = birdeyeLiq;
                dexName = dexName || "Birdeye"; // Indicate source if unknown
                console.log(`[Liquidity] Found via Birdeye: $${liquidityUsd}`);
            }
        }
        // Step 3: Sanity Check with Jupiter Price AND Liquidity Search
        // If we still have $0 liquidity but Jupiter has a price, it's likely valid but we missed the pool.
        // Return -1 to indicate "Unknown/Hidden Liquidity"
        if (liquidityUsd === 0) {
            // Try precise liquidity search first
            const jupLiq = await fetchJupiterLiquidity(mintAddress);
            if (jupLiq > 0) {
                liquidityUsd = jupLiq;
                dexName = "Jupiter Swap";
                console.log(`[Liquidity] Found via Jupiter Search: $${liquidityUsd}`);
            } else {
                // Fallback to price check
                const jupPrice = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$price$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchJupiterPrice"])(mintAddress);
                if (jupPrice && jupPrice.priceUsd > 0) {
                    liquidityUsd = -1; // Special flag for "Unknown"
                    dexName = dexName || "Jupiter";
                    console.log(`[Liquidity] Found Jupiter price ($${jupPrice.priceUsd}) but 0 liquidity. Marking as Unknown.`);
                } else {
                    console.log(`[Liquidity] No Jupiter price found.`);
                }
            }
        }
        // Step 4: Graduated Pump.fun Token Check (Special Handling)
        // If we have 0 liquidity but the address suggests it's a Pump token,
        // check if it has graduated to DEX
        if (liquidityUsd === 0 && mintAddress.toLowerCase().endsWith("pump")) {
            const graduatedData = await checkGraduatedPumpToken(mintAddress);
            if (graduatedData && graduatedData.lpSizeUsd > 0) {
                liquidityUsd = graduatedData.lpSizeUsd;
                dexName = graduatedData.dexName;
                pairAddress = graduatedData.pairAddress;
                rawDexId = graduatedData.dexId;
                console.log(`[Liquidity] Graduated Pump token found on ${dexName}: $${liquidityUsd}`);
            }
        }
        // Step 5: Fallback to Pump.fun API (Last Resort)
        // If even Jupiter fails, check if Pump.fun API reports a market cap.
        if (liquidityUsd === 0) {
            const pumpMC = await fetchPumpFunMarketCap(mintAddress);
            if (pumpMC > 5000) {
                liquidityUsd = -1;
                dexName = dexName || "Pump.fun API";
                console.log(`[Liquidity] Found via Pump.fun API MC: $${pumpMC}. Marking as Unknown.`);
            }
        }
        // Step 6: Nuclear Option - Check Raydium via RPC (Helius)
        // If all APIs are blocked/failing, check on-chain directly.
        if (liquidityUsd === 0) {
            const hasRaydiumPool = await findRaydiumPoolViaRPC(mintAddress);
            if (hasRaydiumPool) {
                liquidityUsd = -1;
                dexName = "Raydium (RPC)";
                console.log(`[Liquidity] Found Raydium pool via RPC. Marking as Unknown.`);
            }
        }
        // Check if LP is burned
        let lpBurned = false;
        // PumpSwap pools always have LP burned — pump.fun graduation process burns LP tokens
        if (rawDexId === "pump" || rawDexId === "pumpswap") {
            lpBurned = true;
            console.log(`[Liquidity] PumpSwap detected (dexId: ${rawDexId}). LP is burned by protocol.`);
        } else if (pairAddress) {
            lpBurned = await checkLpBurned(pairAddress);
        }
        // Try to get additional security info from Birdeye
        await fetchBirdeyeSecurity(mintAddress);
        return {
            lpSizeUsd: liquidityUsd,
            lpLocked: lpBurned,
            lpBurned,
            lockDuration: lpBurned ? "Forever (Burned)" : null,
            dexName,
            pairAddress
        };
    } catch (error) {
        console.error("Error checking liquidity:", error);
        return null;
    }
}
}),
"[project]/lib/external/metadata.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getTokenMetadata",
    ()=>getTokenMetadata
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$liquidity$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/liquidity.ts [app-route] (ecmascript)");
;
;
const HELIUS_API_KEY = process.env.HELIUS_API_KEY;
// Simple in-memory cache for metadata to save RPC credits and speed up
const metadataCache = new Map();
const METADATA_CACHE_TTL = 15 * 60 * 1000; // 15 minutes
// Fetch from Jupiter Token List API
async function fetchJupiterMetadata(mintAddress) {
    // Allow skipping external fetches in offline/dev environments
    if (process.env.SKIP_EXTERNAL_FETCH === "true") return null;
    try {
        const response = await fetch(`https://tokens.jup.ag/token/${mintAddress}`);
        if (!response.ok) {
            return null;
        }
        const data = await response.json();
        return {
            name: data.name || null,
            symbol: data.symbol || null,
            image: data.logoURI || null,
            description: data.extensions?.description || null,
            website: data.extensions?.website || null,
            twitter: data.extensions?.twitter || null,
            telegram: data.extensions?.telegram || null
        };
    } catch (error) {
        console.error("Error fetching from Jupiter:", error);
        return null;
    }
}
// Fetch from Helius DAS API (Metaplex metadata)
async function fetchHeliusMetadata(mintAddress) {
    // Check cache
    const cached = metadataCache.get(mintAddress);
    if (cached && Date.now() - cached.timestamp < METADATA_CACHE_TTL) {
        return cached.data;
    }
    if (!HELIUS_API_KEY) {
        return null;
    }
    try {
        const response = await fetch(`https://mainnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}`, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                jsonrpc: "2.0",
                id: "metadata-request",
                method: "getAsset",
                params: {
                    id: mintAddress
                }
            })
        });
        if (!response.ok) {
            return null;
        }
        const json = await response.json();
        const data = json.result;
        if (!data) {
            return null;
        }
        // Fetch off-chain metadata if json_uri exists
        let offChainData = {};
        if (data.content?.json_uri) {
            try {
                const metadataResponse = await fetch(data.content.json_uri);
                if (metadataResponse.ok) {
                    offChainData = await metadataResponse.json();
                }
            } catch  {
            // Ignore off-chain fetch errors
            }
        }
        const result = {
            name: data.content?.metadata?.name || offChainData.name || null,
            symbol: data.content?.metadata?.symbol || offChainData.symbol || null,
            image: data.content?.links?.image || offChainData.image || null,
            description: data.content?.metadata?.description || offChainData.description || null,
            website: data.content?.links?.external_url || offChainData.external_url || null,
            twitter: offChainData.twitter || null,
            telegram: offChainData.telegram || null,
            isMutable: data.mutable ?? true,
            creators: data.creators || []
        };
        // Cache the successful result
        metadataCache.set(mintAddress, {
            data: result,
            timestamp: Date.now()
        });
        return result;
    } catch (error) {
        console.error("Error fetching from Helius:", error);
        return null;
    }
}
// Fetch from DexScreener Token Profiles API (V1)
// This is separate from the Pairs API and often has better metadata (icons/links)
async function fetchDexScreenerProfile(mintAddress) {
    try {
        const response = await fetch(`https://api.dexscreener.com/token-profiles/latest/v1`, {
            method: "GET",
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (!response.ok) return null;
        // The API returns an array of profiles. Since we can't filter by token address in the URL for this specific endpoint (it's "latest"),
        // this might not be the right endpoint for *specific* token lookup unless we want to search the latest list.
        // WAIT: The user provided specific JSON.
        // Usually there is `https://api.dexscreener.com/token-profiles/latest/v1`.
        // But is there `https://api.dexscreener.com/token-profiles/latest/v1/{chainId}/{tokenAddress}`?
        // Let's try that one as it's more standard for "profiles by address".
        // Attempting direct profile fetch by address
        const profileResponse = await fetch(`https://api.dexscreener.com/token-profiles/latest/v1/solana/${mintAddress}`, {
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
            }
        });
        if (profileResponse.ok) {
            const profiles = await profileResponse.json();
            // It returns an array of profiles, usually length 1 if found
            if (Array.isArray(profiles) && profiles.length > 0) {
                const p = profiles[0];
                return {
                    image: p.icon || null,
                    description: p.description || null,
                    website: p.links?.find((l)=>l.type === 'website')?.url || null,
                    twitter: p.links?.find((l)=>l.type === 'twitter')?.url || null,
                    telegram: p.links?.find((l)=>l.type === 'telegram')?.url || null
                };
            }
        }
        return null;
    } catch (e) {
        console.warn("DexScreener Profile fetch failed:", e);
        return null;
    }
}
async function fetchPumpFunMetadata(mintAddress) {
    if (process.env.SKIP_EXTERNAL_FETCH === "true") return null;
    try {
        const response = await fetch(`https://frontend-api.pump.fun/coins/${mintAddress}`, {
            method: "GET",
            headers: {
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                "Accept": "application/json"
            }
        });
        if (!response.ok) {
            return null;
        }
        const data = await response.json();
        if (!data) return null;
        return {
            name: data.name || null,
            symbol: data.symbol || null,
            image: data.image_uri || null,
            description: data.description || null,
            twitter: data.twitter || null,
            telegram: data.telegram || null,
            website: data.website || null,
            createdAt: data.created_timestamp ? new Date(data.created_timestamp).toISOString() : null,
            creators: data.creator ? [
                {
                    address: data.creator,
                    share: 100,
                    verified: false
                }
            ] : []
        };
    } catch (error) {
        console.warn("Pump.fun API fetch failed:", error);
        return null;
    }
}
// Check isMutable using on-chain Metaplex metadata account
async function checkMetadataIsMutable(mintAddress) {
    if (!HELIUS_API_KEY) {
        return true; // Assume mutable if we can't check
    }
    try {
        // Derive Metaplex metadata PDA
        const METADATA_PROGRAM_ID = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"]("metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s");
        const mintPubkey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        const [metadataPDA] = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"].findProgramAddressSync([
            Buffer.from("metadata"),
            METADATA_PROGRAM_ID.toBuffer(),
            mintPubkey.toBuffer()
        ], METADATA_PROGRAM_ID);
        // Fetch raw account data
        const response = await fetch(`https://mainnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}`, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                jsonrpc: "2.0",
                id: "metadata-account",
                method: "getAccountInfo",
                params: [
                    metadataPDA.toBase58(),
                    {
                        encoding: "base64"
                    }
                ]
            })
        });
        if (!response.ok) {
            return true;
        }
        const json = await response.json();
        const accountData = json.result?.value?.data;
        if (!accountData || !accountData[0]) {
            return true;
        }
        // Decode base64 and check isMutable flag
        // isMutable is at a specific offset in Metaplex metadata account
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        const _data = Buffer.from(accountData[0], "base64");
        // The isMutable flag is near the end of the fixed portion
        // After: key (1) + update_authority (32) + mint (32) + name (36) + symbol (14) + uri (204) + seller_fee (2) + creators option
        // This is simplified - in production you'd want proper deserialization
        // For Token Metadata v1.1+, isMutable is at offset 1 + 32 + 32 + 4 + name_len + 4 + symbol_len + 4 + uri_len + 2 + creators...
        // Simplified check: isMutable is typically byte at position after all the variable data
        // A proper implementation would use @metaplex-foundation/mpl-token-metadata
        // For now, we rely on Helius DAS API response which includes mutable field
        return true;
    } catch (error) {
        console.error("Error checking isMutable:", error);
        return true;
    }
}
async function getTokenMetadata(mintAddress) {
    try {
        // Validate address
        new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        // Fetch from multiple sources in parallel (including pump.fun)
        const [heliusData, pumpFunData, dexData, dexProfileData] = await Promise.all([
            fetchHeliusMetadata(mintAddress),
            fetchPumpFunMetadata(mintAddress),
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$liquidity$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchDexScreener"])(mintAddress),
            fetchDexScreenerProfile(mintAddress)
        ]);
        // Merge data, preferring Helius (more complete) but falling back to others
        const metadata = {
            // Prefer Helius, then pump.fun, then DexScreener (Profile), then DexScreener (Pairs)
            name: heliusData?.name || pumpFunData?.name || dexData?.name || null,
            symbol: heliusData?.symbol || pumpFunData?.symbol || dexData?.symbol || null,
            image: heliusData?.image || pumpFunData?.image || dexProfileData?.image || dexData?.image || null,
            description: heliusData?.description || pumpFunData?.description || dexProfileData?.description || null,
            website: heliusData?.website || pumpFunData?.website || dexProfileData?.website || null,
            twitter: heliusData?.twitter || pumpFunData?.twitter || dexProfileData?.twitter || null,
            telegram: heliusData?.telegram || pumpFunData?.telegram || dexProfileData?.telegram || null,
            isMutable: heliusData?.isMutable ?? true,
            createdAt: heliusData?.createdAt || pumpFunData?.createdAt || null,
            creators: heliusData?.creators || pumpFunData?.creators || []
        };
        // If we couldn't get isMutable from Helius, try direct check
        if (heliusData?.isMutable === undefined) {
            metadata.isMutable = await checkMetadataIsMutable(mintAddress);
        }
        return metadata;
    } catch (error) {
        console.error("Error getting token metadata:", error);
        return null;
    }
}
}),
"[project]/lib/solana/pumpfun.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__,
    "getPumpFunCurveState",
    ()=>getPumpFunCurveState
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
;
;
const PUMP_FUN_PROGRAM_ID = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"]("6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P");
const LAMPORTS_PER_SOL = 1_000_000_000;
// Pump.fun Constants
const INITIAL_VIRTUAL_TOKEN_RESERVES = BigInt("1073000000000000"); // 1.073B tokens with 6 decimals
const TOTAL_TOKENS_FOR_SALE = BigInt("793100000000000"); // 793.1M tokens available to buy (standard for 100% progress)
const GRADUATION_REAL_SOL_TARGET = 85; // Target in real SOL collected
async function getPumpFunCurveState(mintAddress) {
    try {
        const mintPk = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        const [pda] = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"].findProgramAddressSync([
            Buffer.from("bonding-curve"),
            mintPk.toBuffer()
        ], PUMP_FUN_PROGRAM_ID);
        const pdaInfo = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getAccountInfo(pda);
        if (!pdaInfo || !pdaInfo.data) {
            return {
                exists: false,
                pda: pda.toBase58(),
                virtualTokenReserves: "0",
                virtualSolReservesLamports: "0",
                virtualSolReservesSol: 0,
                realTokenReserves: "0",
                realSolReservesLamports: "0",
                realSolReservesSol: 0,
                tokenTotalSupply: 0,
                complete: false,
                pricePerTokenSol: null,
                marketCapSol: null,
                curveProgressPercent: null,
                remainingSolToGraduate: null
            };
        }
        const buf = Buffer.from(pdaInfo.data);
        const START_OFFSET = 8; // Anchor discriminator
        const REQUIRED_SIZE = START_OFFSET + 41; // 8 (discriminator) + 5 * 8 (u64 fields) + 1 (bool) = 49 bytes
        // Guard: If account data is too small, it's not a valid Pump.fun bonding curve
        if (buf.length < REQUIRED_SIZE) {
            return {
                exists: false,
                pda: pda.toBase58(),
                virtualTokenReserves: "0",
                virtualSolReservesLamports: "0",
                virtualSolReservesSol: 0,
                realTokenReserves: "0",
                realSolReservesLamports: "0",
                realSolReservesSol: 0,
                tokenTotalSupply: 0,
                complete: false,
                pricePerTokenSol: null,
                marketCapSol: null,
                curveProgressPercent: null,
                remainingSolToGraduate: null
            };
        }
        // Decoding
        const virtualTokenReserves = buf.readBigUInt64LE(START_OFFSET + 0);
        const virtualSolReservesLamports = buf.readBigUInt64LE(START_OFFSET + 8);
        const realTokenReserves = buf.readBigUInt64LE(START_OFFSET + 16);
        const realSolReservesLamports = buf.readBigUInt64LE(START_OFFSET + 24);
        const tokenTotalSupply = buf.readBigUInt64LE(START_OFFSET + 32);
        const complete = Boolean(buf.readUInt8(START_OFFSET + 40));
        const virtualSolReservesSol = Number(virtualSolReservesLamports) / LAMPORTS_PER_SOL;
        const realSolReservesSol = Number(realSolReservesLamports) / LAMPORTS_PER_SOL;
        // 1. Calculate Progress by TOKENS
        const tokensSold = INITIAL_VIRTUAL_TOKEN_RESERVES - virtualTokenReserves;
        const progress = Number(tokensSold * BigInt(10000) / TOTAL_TOKENS_FOR_SALE) / 100;
        // 2. Market Cap calculation (Corrected)
        // Formula: (Virtual SOL / Virtual Tokens) * Total Supply * (10^6 / 10^9)
        // Simplified: (Virtual SOL Lamports / Virtual Tokens) * 10^9 / 10^9 = Price in Lamports per RAW token
        // MC in SOL = (virtualSolReservesSol / (Number(virtualTokenReserves) / 1e6)) * 1,000,000,000
        let marketCapSol = 0;
        if (virtualTokenReserves > BigInt(0)) {
            const pricePerTokenSol = virtualSolReservesSol / (Number(virtualTokenReserves) / 1_000_000);
            marketCapSol = pricePerTokenSol * 1_000_000_000;
        }
        // 3. Remaining SOL to graduate
        const remainingSol = Math.max(0, GRADUATION_REAL_SOL_TARGET - realSolReservesSol);
        return {
            exists: true,
            pda: pda.toBase58(),
            virtualTokenReserves: virtualTokenReserves.toString(),
            virtualSolReservesLamports: virtualSolReservesLamports.toString(),
            virtualSolReservesSol,
            realTokenReserves: realTokenReserves.toString(),
            realSolReservesLamports: realSolReservesLamports.toString(),
            realSolReservesSol,
            tokenTotalSupply: 1_000_000_000,
            complete,
            pricePerTokenSol: marketCapSol / 1_000_000_000,
            marketCapSol,
            curveProgressPercent: complete ? 100 : Math.min(99.9, progress),
            remainingSolToGraduate: complete ? 0 : remainingSol
        };
    } catch (error) {
        console.error("getPumpFunCurveState error:", error);
        return null;
    }
}
const __TURBOPACK__default__export__ = getPumpFunCurveState;
}),
"[project]/lib/solana/tokenAge.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "checkTokenAge",
    ()=>checkTokenAge
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
;
;
// Known token creation dates (for major tokens that are hard to fetch)
const KNOWN_CREATION_DATES = {
    // USDC - launched November 2020
    "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v": new Date("2020-11-09"),
    // USDT
    "Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB": new Date("2020-09-14"),
    // Wrapped SOL
    "So11111111111111111111111111111111111111112": new Date("2020-03-16"),
    // BONK
    "DezXAZ8z7PnrnRJjz3wXBoRgixCa6xjnB7YaB1pPB263": new Date("2022-12-25"),
    // JUP
    "JUPyiwrYJFskUPiHa7hkeR8VUtAeFoSYbKedZNsDvCN": new Date("2024-01-31"),
    // JTO
    "jtojtomepa8beP8AuQc6eXt5FriJwfFMwQx2v2f9mCL": new Date("2023-12-07"),
    // PYTH
    "HZ1JovNiVvGrGNiiYvEozEVgZ58xaU3RKwX8eACQBCt3": new Date("2023-11-20"),
    // RAY
    "4k3Dyjzvzp8eMZWUXbBCjEvwSkkk59S5iCNLY3QrkX6R": new Date("2021-02-21"),
    // ORCA
    "orcaEKTdK7LKz57vaAYr9QeNsVEPfiu6QeMU1kektZE": new Date("2021-08-09"),
    // mSOL
    "mSoLzYCxHdYgdzU16g5QSh3i5K3z3KZK7ytfqcJm7So": new Date("2021-08-04")
};
async function checkTokenAge(mintAddress, pairCreatedAt) {
    try {
        // 0. Use DexScreener pair creation date if available (reliable for traded tokens)
        if (pairCreatedAt) {
            return calculateAge(new Date(pairCreatedAt));
        }
        // Check known dates first
        const knownDate = KNOWN_CREATION_DATES[mintAddress];
        if (knownDate) {
            return calculateAge(knownDate);
        }
        // 1. Try Helius DAS (most reliable for indexed tokens)
        const heliusAge = await getAgeFromAccountInfo(mintAddress);
        if (heliusAge) return heliusAge;
        // 2. Try Pump.fun API (accurate for pump tokens)
        if (mintAddress.endsWith("pump")) {
            try {
                const res = await fetch(`https://frontend-api.pump.fun/coins/${mintAddress}`, {
                    headers: {
                        "User-Agent": "RugSolScanner/1.0"
                    }
                });
                if (res.ok) {
                    const data = await res.json();
                    if (data.created_timestamp) {
                        return calculateAge(new Date(data.created_timestamp));
                    }
                }
            } catch  {}
        }
        const mintPublicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](mintAddress);
        // 3. Fallback: Signatures
        // Try to get signatures - for newer tokens this works
        const signatures = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["connection"].getSignaturesForAddress(mintPublicKey, {
            limit: 1000
        }, "confirmed");
        if (!signatures || signatures.length === 0) {
            return null;
        }
        // The last signature in the array is usually the oldest
        const oldestSignature = signatures[signatures.length - 1];
        // If we have 1000 signatures, we haven't reached the beginning of history.
        // However, the age of the 1000th transaction gives us a "lower bound".
        // If the 1000th transaction was 2 days ago, the token is AT LEAST 2 days old.
        // This is sufficient to pass the "New Token (<24h)" check.
        const oldestTime = oldestSignature.blockTime;
        if (oldestTime) {
            return calculateAge(new Date(oldestTime * 1000));
        }
        return null;
    } catch (error) {
        return null;
    }
}
function calculateAge(createdAt) {
    const now = new Date();
    const ageInMs = now.getTime() - createdAt.getTime();
    const ageInHours = Math.floor(ageInMs / (1000 * 60 * 60));
    const ageInDays = Math.floor(ageInHours / 24);
    return {
        createdAt,
        ageInDays,
        ageInHours,
        isNew: ageInHours < 24
    };
}
// Fallback: try to get age from Helius DAS API
async function getAgeFromAccountInfo(mintAddress) {
    const heliusApiKey = process.env.HELIUS_API_KEY;
    if (!heliusApiKey) return null;
    try {
        const response = await fetch(`https://mainnet.helius-rpc.com/?api-key=${heliusApiKey}`, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                jsonrpc: "2.0",
                id: "get-asset",
                method: "getAsset",
                params: {
                    id: mintAddress
                }
            })
        });
        const data = await response.json();
        if (data.result?.content?.metadata?.created_at) {
            const createdAt = new Date(data.result.content.metadata.created_at);
            return calculateAge(createdAt);
        }
        return null;
    } catch  {
        return null;
    }
}
}),
"[project]/lib/scoring/whitelist.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Whitelist of known legitimate tokens that should not be penalized
 * for having mint/freeze authority or other "risky" characteristics
 */ __turbopack_context__.s([
    "WHITELISTED_TOKENS",
    ()=>WHITELISTED_TOKENS,
    "WHITELIST_MAP",
    ()=>WHITELIST_MAP,
    "getWhitelistedToken",
    ()=>getWhitelistedToken,
    "isWhitelisted",
    ()=>isWhitelisted
]);
const WHITELISTED_TOKENS = [
    // Stablecoins
    {
        address: "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",
        name: "USD Coin",
        symbol: "USDC",
        type: "stablecoin",
        image: "https://raw.githubusercontent.com/solana-labs/token-list/main/assets/mainnet/EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v/logo.png",
        skipChecks: {
            mintAuthority: true,
            freezeAuthority: true,
            age: true,
            liquidity: true,
            metadata: true
        },
        minScore: 95
    },
    {
        address: "Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB",
        name: "USDT",
        symbol: "USDT",
        type: "stablecoin",
        image: "https://raw.githubusercontent.com/solana-labs/token-list/main/assets/mainnet/Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB/logo.png",
        skipChecks: {
            mintAuthority: true,
            freezeAuthority: true,
            age: true,
            liquidity: true,
            metadata: true
        },
        minScore: 90
    },
    // Wrapped SOL
    {
        address: "So11111111111111111111111111111111111111112",
        name: "Wrapped SOL",
        symbol: "SOL",
        type: "wrapped",
        image: "https://raw.githubusercontent.com/solana-labs/token-list/main/assets/mainnet/So11111111111111111111111111111111111111112/logo.png",
        skipChecks: {
            mintAuthority: true,
            freezeAuthority: true,
            holders: true,
            age: true,
            liquidity: true,
            metadata: true
        },
        minScore: 100
    },
    {
        address: "So11111111111111111111111111111111111111111",
        name: "Solana",
        symbol: "SOL",
        type: "wrapped",
        image: "https://raw.githubusercontent.com/solana-labs/token-list/main/assets/mainnet/So11111111111111111111111111111111111111112/logo.png",
        skipChecks: {
            mintAuthority: true,
            freezeAuthority: true,
            holders: true,
            age: true,
            liquidity: true,
            metadata: true
        },
        minScore: 100
    },
    // Solana Mobile
    {
        address: "SKRbvo6Gf7GondiT3BbTfuRDPqLWei4j2Qy2NPGZhW3",
        name: "Seeker",
        symbol: "SKR",
        type: "major",
        skipChecks: {
            mintAuthority: true,
            freezeAuthority: true,
            holders: true,
            age: true,
            liquidity: true,
            metadata: true
        },
        minScore: 85
    },
    // Major tokens
    {
        address: "JUPyiwrYJFskUPiHa7hkeR8VUtAeFoSYbKedZNsDvCN",
        name: "Jupiter",
        symbol: "JUP",
        type: "major",
        skipChecks: {
            age: true
        },
        minScore: 85
    },
    {
        address: "jtojtomepa8beP8AuQc6eXt5FriJwfFMwQx2v2f9mCL",
        name: "Jito",
        symbol: "JTO",
        type: "major",
        skipChecks: {
            age: true
        },
        minScore: 85
    },
    {
        address: "orcaEKTdK7LKz57vaAYr9QeNsVEPfiu6QeMU1kektZE",
        name: "Orca",
        symbol: "ORCA",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 85
    },
    {
        address: "RaszyivZBGpvFmKWRqpvH6ijgP5xeyTZReKrPy4dTKi",
        name: "Raydium",
        symbol: "RAY",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 85
    },
    {
        address: "4k3Dyjzvzp8eMZWUXbBCjEvwSkkk59S5iCNLY3QrkX6R",
        name: "Raydium",
        symbol: "RAY",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 85
    },
    {
        address: "mSoLzYCxHdYgdzU16g5QSh3i5K3z3KZK7ytfqcJm7So",
        name: "Marinade Staked SOL",
        symbol: "mSOL",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 90
    },
    {
        address: "7dHbWXmci3dT8UFYWYZweBLXgycu7Y3iL6trKn1Y7ARj",
        name: "Lido Staked SOL",
        symbol: "stSOL",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 90
    },
    {
        address: "bSo13r4TkiE4KumL71LsHTPpL2euBYLFx6h9HP3piy1",
        name: "BlazeStake Staked SOL",
        symbol: "bSOL",
        type: "defi",
        skipChecks: {
            mintAuthority: true,
            age: true
        },
        minScore: 85
    },
    {
        address: "HZ1JovNiVvGrGNiiYvEozEVgZ58xaU3RKwX8eACQBCt3",
        name: "Pyth Network",
        symbol: "PYTH",
        type: "major",
        skipChecks: {
            age: true
        },
        minScore: 85
    },
    {
        address: "DezXAZ8z7PnrnRJjz3wXBoRgixCa6xjnB7YaB1pPB263",
        name: "Bonk",
        symbol: "BONK",
        type: "major",
        image: "https://raw.githubusercontent.com/solana-labs/token-list/main/assets/mainnet/DezXAZ8z7PnrnRJjz3wXBoRgixCa6xjnB7YaB1pPB263/logo.png",
        skipChecks: {
            age: true,
            metadata: true,
            liquidity: true
        },
        minScore: 85
    },
    {
        address: "WENWENvqqNya429ubCdR81ZmD69brwQaaBYY6p3LCpk",
        name: "Wen",
        symbol: "WEN",
        type: "major",
        skipChecks: {
            age: true
        },
        minScore: 70
    }
];
const WHITELIST_MAP = new Map(WHITELISTED_TOKENS.map((token)=>[
        token.address,
        token
    ]));
function isWhitelisted(address) {
    return WHITELIST_MAP.has(address);
}
function getWhitelistedToken(address) {
    return WHITELIST_MAP.get(address);
}
}),
"[project]/lib/external/jupiter-tokens.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Jupiter Verified Token List
 * Automatically fetches and caches verified tokens from Jupiter
 */ __turbopack_context__.s([
    "fetchVerifiedTokens",
    ()=>fetchVerifiedTokens,
    "getJupiterTokenInfo",
    ()=>getJupiterTokenInfo,
    "getTokenType",
    ()=>getTokenType,
    "isJupiterVerified",
    ()=>isJupiterVerified
]);
// Cache for verified tokens
let verifiedTokensCache = null;
let cacheTimestamp = 0;
const CACHE_TTL = 30 * 60 * 1000; // 30 minutes
async function fetchVerifiedTokens() {
    const now = Date.now();
    // Return cached data if still valid
    if (verifiedTokensCache && now - cacheTimestamp < CACHE_TTL) {
        return verifiedTokensCache;
    }
    try {
        // Jupiter strict list - only verified tokens
        const response = await fetch("https://token.jup.ag/strict", {
            headers: {
                "Accept": "application/json"
            },
            next: {
                revalidate: 1800
            }
        });
        if (!response.ok) {
            console.error("Failed to fetch Jupiter token list:", response.status);
            return verifiedTokensCache || new Map();
        }
        const tokens = await response.json();
        // Build map for fast lookup
        const tokenMap = new Map();
        for (const token of tokens){
            tokenMap.set(token.address, token);
        }
        verifiedTokensCache = tokenMap;
        cacheTimestamp = now;
        console.log(`Loaded ${tokenMap.size} verified tokens from Jupiter`);
        return tokenMap;
    } catch (error) {
        // Silently fail or log as debug info to avoid console clutter
        // console.debug("Jupiter token list unavailable:", error);
        return verifiedTokensCache || new Map();
    }
}
async function isJupiterVerified(address) {
    const tokens = await fetchVerifiedTokens();
    return tokens.has(address);
}
async function getJupiterTokenInfo(address) {
    const tokens = await fetchVerifiedTokens();
    return tokens.get(address) || null;
}
function getTokenType(token) {
    const tags = token.tags || [];
    const name = token.name.toLowerCase();
    const symbol = token.symbol.toLowerCase();
    // Stablecoins
    if (tags.includes("stablecoin") || symbol.includes("usd") || symbol.includes("usdt") || symbol.includes("usdc") || symbol.includes("dai")) {
        return "stablecoin";
    }
    // Wrapped tokens
    if (tags.includes("wrapped") || name.includes("wrapped") || symbol.startsWith("w")) {
        return "wrapped";
    }
    // LST (Liquid Staking Tokens)
    if (tags.includes("lst") || name.includes("staked sol") || symbol.includes("sol")) {
        return "defi";
    }
    // Meme tokens (usually have high volume but simple names)
    if (tags.includes("meme") || tags.includes("community")) {
        return "meme";
    }
    // DeFi tokens
    if (tags.includes("defi") || tags.includes("governance")) {
        return "defi";
    }
    // If it has significant daily volume, it's likely major
    if (token.daily_volume && token.daily_volume > 1000000) {
        return "major";
    }
    return "unknown";
}
}),
"[project]/lib/scoring/constants.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GRADES",
    ()=>GRADES,
    "PUMP_PROGRAM_ID",
    ()=>PUMP_PROGRAM_ID
]);
const GRADES = {
    A: {
        min: 80,
        color: "#22c55e",
        label: "Safe"
    },
    B: {
        min: 60,
        color: "#84cc16",
        label: "Low Risk"
    },
    C: {
        min: 40,
        color: "#eab308",
        label: "Moderate Risk"
    },
    D: {
        min: 20,
        color: "#f97316",
        label: "High Risk"
    },
    F: {
        min: 0,
        color: "#ef4444",
        label: "Likely Scam"
    }
};
const PUMP_PROGRAM_ID = "6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P";
}),
"[project]/lib/scoring/engine.ts [app-route] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "calculatePenalties",
    ()=>calculatePenalties,
    "scanToken",
    ()=>scanToken
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$price$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/price.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$jupiter$2d$swap$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/jupiter-swap.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$authority$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/authority.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$holders$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/holders.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$liquidity$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/liquidity.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$metadata$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/metadata.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$pumpfun$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/pumpfun.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$tokenAge$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/tokenAge.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$whitelist$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/scoring/whitelist.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$jupiter$2d$tokens$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/external/jupiter-tokens.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/solana/connection.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/scoring/constants.ts [app-route] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
;
;
// --- Helpers ---
async function safeCheck(checkFn, checkName) {
    try {
        const result = await checkFn();
        return {
            status: result === null ? "unknown" : "success",
            data: result
        };
    } catch (error) {
        console.error(`Error in ${checkName}:`, error);
        return {
            status: "error",
            data: null,
            error: error instanceof Error ? error.message : "Unknown error"
        };
    }
}
function getGrade(score, hasCriticalFail) {
    if (hasCriticalFail) return {
        grade: "F",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].F
    };
    if (score >= __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].A.min) return {
        grade: "A",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].A
    };
    if (score >= __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].B.min) return {
        grade: "B",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].B
    };
    if (score >= __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].C.min) return {
        grade: "C",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].C
    };
    if (score >= __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].D.min) return {
        grade: "D",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].D
    };
    return {
        grade: "F",
        ...__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$constants$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GRADES"].F
    };
}
// --- Analysis Helpers ---
function analyzeDevWallet(metadata, holders) {
    if (!metadata?.creators || metadata.creators.length === 0 || !holders) {
        return {};
    }
    // Assuming the first creator is the dev/deployer
    const devAddress = metadata.creators[0].address;
    // Find dev in holders
    const devHolder = holders.topHolders.find((h)=>h.owner === devAddress);
    const devBalancePercent = devHolder ? devHolder.percent : 0;
    // Heuristic: If dev holds < 1% and is not the mint authority (handled elsewhere), likely sold
    // For Pump.fun: Dev usually buys ~5-10% initially. If they have < 1%, they dumped.
    const isDevSoldOut = devBalancePercent < 1;
    return {
        devAddress,
        devBalancePercent,
        isDevSoldOut
    };
}
async function detectTokenMode(tokenAddress, pumpState, context, isWhitelisted = false) {
    // Hard override: Whitelisted tokens or wSOL are never Pump.fun tokens
    const WSOL_ADDRESS = "So11111111111111111111111111111111111111112";
    if (isWhitelisted || tokenAddress === WSOL_ADDRESS) {
        return {
            mode: "dex",
            dexData: null
        };
    }
    // Method 0: Mint Authority Check (Ultimate Source of Truth)
    // If the Mint Authority is the Bonding Curve PDA, the token is 100% on the curve.
    try {
        const mintPk = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](tokenAddress);
        const PUMP_PROGRAM = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"]("6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P");
        const [bondingCurvePda] = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"].findProgramAddressSync([
            Buffer.from("bonding-curve"),
            mintPk.toBuffer()
        ], PUMP_PROGRAM);
        if (context) {
            const authority = context.mintAuthority;
            // Pump.fun tokens on the curve ALWAYS have the Bonding Curve PDA as the Mint Authority
            if (authority === bondingCurvePda.toBase58()) {
                console.log(`[Mode] Mint Authority matches Bonding Curve PDA. Force PUMP mode.`);
                return {
                    mode: "pump",
                    dexData: null
                };
            }
        }
    } catch (e) {
        console.warn("Mint Authority check failed:", e);
    }
    // Method 1: Check bonding curve status (from previous step)
    if (pumpState && pumpState.exists) {
        if (!pumpState.complete) {
            return {
                mode: "pump",
                dexData: null
            };
        }
        // Graduated from bonding curve → now on DEX
        console.log(`[Mode] Bonding curve is complete. Switching to DEX mode.`);
        return {
            mode: "dex",
            dexData: null
        };
    }
    // Method 2: Check for DEX pools via DexScreener (Fallback)
    try {
        const dexData = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$liquidity$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchDexScreener"])(tokenAddress);
        if (dexData) {
            // If DexScreener found it on Pump.fun and the address ends with "pump",
            // but we couldn't confirm bonding curve is active, treat as graduated
            if (dexData.dexName === "Pump.fun" && tokenAddress.toLowerCase().endsWith("pump")) {
                // Token address ends with "pump" but DexScreener shows it on Pump.fun (PumpSwap)
                // This is a graduated token trading on Pump.fun DEX
                console.log(`[Mode] Pump.fun address trading on PumpSwap DEX. DEX mode.`);
                return {
                    mode: "dex",
                    dexData
                };
            }
            // If DexScreener reports Pump.fun and NO bonding curve state found, it's graduated
            if (dexData.dexName === "Pump.fun" && !pumpState?.exists) {
                console.log(`[Mode] Found on Pump.fun DEX with no active bonding curve. DEX mode.`);
                return {
                    mode: "dex",
                    dexData
                };
            }
            if (dexData.pairAddress) {
                return {
                    mode: "dex",
                    dexData
                };
            }
        }
    } catch (err) {
        console.error("Mode detection DexScreener error:", err);
    }
    // Method 3: Address Pattern Fallback
    // If we couldn't determine mode via RPC or API, but the address ends in "pump",
    // it's almost certainly a Pump.fun token (likely new/unindexed).
    // defaulting to DEX here would cause a "No Liquidity" panic.
    if (tokenAddress.toLowerCase().endsWith("pump")) {
        console.log(`[Mode] Address ends with 'pump'. Defaulting to PUMP mode.`);
        return {
            mode: "pump",
            dexData: null
        };
    }
    // Default to DEX mode (safest fallback for non-pump addresses)
    return {
        mode: "dex",
        dexData: null
    };
}
function calculatePenalties(checks, mode, whitelist) {
    const penalties = [];
    const skip = whitelist?.skipChecks || {};
    // -- Critical Checks (Auto-F triggers) --
    // 1. Honeypot (Critical)
    // Skip for Pump.fun tokens — Jupiter has no routes for bonding curve tokens,
    // so sell simulation always fails (false positive).
    if (!skip.liquidity && mode !== "pump" && checks.honeypot.data?.isHoneypot) {
        penalties.push({
            category: "Critical",
            reason: `Sell simulation failed: ${checks.honeypot.data.reason || "Unable to sell"}`,
            points: 100,
            isCritical: true
        });
    }
    // 2. Mint Authority (Critical)
    // Skip for Pump.fun tokens — on the bonding curve, the Mint Authority is always
    // the Bonding Curve PDA. This is expected and not a risk.
    if (!skip.mintAuthority && mode !== "pump" && checks.mintAuthority.data?.status === "fail") {
        penalties.push({
            category: "Critical",
            reason: "Mint authority is NOT revoked",
            points: 50,
            isCritical: true
        });
    }
    // 3. Freeze Authority (Critical)
    // Skip for Pump.fun tokens — Pump.fun tokens typically have freeze authority
    // set to the program, which is expected behavior.
    if (!skip.freezeAuthority && mode !== "pump" && checks.freezeAuthority.data?.status === "fail") {
        penalties.push({
            category: "Critical",
            reason: "Freeze authority is NOT revoked",
            points: 30,
            isCritical: true
        });
    }
    // -- High Risk --
    // 4. Dev Action (Dev Sold)
    if (checks.advanced?.data) {
        const { isDevSoldOut, devBalancePercent } = checks.advanced.data;
        // On Pump.fun, if dev is out, it's very bad. On DEX, it might be community takeover (CTO), but still risky if recent.
        // We treat "Dev Sold Out" as high risk unless verified community.
        if (isDevSoldOut) {
            penalties.push({
                category: "Dev Activity",
                reason: `Dev has sold out (holds ${devBalancePercent?.toFixed(2)}%)`,
                points: mode === "pump" ? 40 : 25
            });
        } else if (devBalancePercent) {
            // Pump Mode Specific: Dev Holdings
            if (mode === "pump") {
                if (devBalancePercent > 10) {
                    penalties.push({
                        category: "Dev Wallet",
                        reason: `Dev holds high supply (${devBalancePercent.toFixed(1)}%)`,
                        points: 25
                    });
                } else if (devBalancePercent > 5) {
                    penalties.push({
                        category: "Dev Wallet",
                        reason: `Dev holds significant supply (${devBalancePercent.toFixed(1)}%)`,
                        points: 15
                    });
                }
            } else {
                // DEX Mode
                if (devBalancePercent > 30) {
                    penalties.push({
                        category: "Dev Activity",
                        reason: `Dev wallet holds ${devBalancePercent.toFixed(1)}% (Dump risk)`,
                        points: 20
                    });
                }
            }
        }
    }
    // 5. Liquidity
    if (!skip.liquidity && checks.liquidity.data) {
        const liq = checks.liquidity.data;
        if (mode === "pump") {
        // Pump Mode: No liquidity penalty (bonding curve).
        // We check Bonding Curve Progress instead (informational mostly, or warning if very low)
        // Implementation note: The progress is part of metadata/advanced data, but we can't easily penalize here without passing it in.
        // However, the prompt says "Bonding Curve Progress... < 10% filled: show warning". 
        // We'll leave it as UI warning for now to avoid over-penalizing new tokens.
        } else {
            // DEX Mode: Strict checks
            if (liq.lpSizeUsd === -1) {
                penalties.push({
                    category: "Low Liquidity",
                    reason: "Liquidity data unavailable (Unknown)",
                    points: 10
                });
            } else if (liq.lpSizeUsd === 0) {
                penalties.push({
                    category: "Low Liquidity",
                    reason: "No Liquidity (Pool Size $0)",
                    points: 50,
                    isCritical: true
                });
            } else if (liq.lpSizeUsd < 1000) {
                penalties.push({
                    category: "Low Liquidity",
                    reason: `Liquidity is critically low ($${liq.lpSizeUsd.toLocaleString()})`,
                    points: 30
                });
            } else if (liq.lpSizeUsd < 10000) {
                penalties.push({
                    category: "Low Liquidity",
                    reason: `Liquidity is low ($${liq.lpSizeUsd.toLocaleString()})`,
                    points: 20
                });
            } else if (liq.lpSizeUsd < 50000) {
                penalties.push({
                    category: "Low Liquidity",
                    reason: `Liquidity is moderate ($${liq.lpSizeUsd.toLocaleString()})`,
                    points: 10
                });
            }
        // if (!liq.lpBurned && !liq.lpLocked) {
        //   penalties.push({ category: "LP Not Locked", reason: "Liquidity is not burned or locked", points: 30 });
        // }
        }
    }
    // 6. Holders (Medium to High Risk)
    if (!skip.holders && checks.holders.data) {
        const { topTenPercent, largestHolder } = checks.holders.data;
        // Logic: Calculate Top 10 penalty and Single Holder penalty independently, 
        // then take the MAX of them to avoid double counting.
        let top10Penalty = 0;
        let top10Reason = "";
        let singlePenalty = 0;
        let singleReason = "";
        if (mode === "pump") {
            // Pump Mode
            if (topTenPercent > 50) {
                top10Penalty = 25;
                top10Reason = `High concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            } else if (topTenPercent > 35) {
                top10Penalty = 15;
                top10Reason = `Moderate concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            }
            if (largestHolder && largestHolder.percent > 20) {
                singlePenalty = 20;
                singleReason = `Whale risk (Single holder owns ${largestHolder.percent.toFixed(1)}%)`;
            }
        } else {
            // DEX Mode (Stricter)
            // New Thresholds: >80% (-50), >60% (-35), >50% (-25), >40% (-15), >30% (-10)
            if (topTenPercent > 80) {
                top10Penalty = 50;
                top10Reason = `Extreme concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            } else if (topTenPercent > 60) {
                top10Penalty = 35;
                top10Reason = `Very high concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            } else if (topTenPercent > 50) {
                top10Penalty = 25;
                top10Reason = `High concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            } else if (topTenPercent > 40) {
                top10Penalty = 15;
                top10Reason = `Moderate concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            } else if (topTenPercent > 30) {
                top10Penalty = 10;
                top10Reason = `Elevated concentration (Top 10 hold ${topTenPercent.toFixed(1)}%)`;
            }
            // Largest Holder Thresholds: >50% (-40), >30% (-30), >20% (-20), >10% (-10)
            if (largestHolder) {
                if (largestHolder.percent > 50) {
                    singlePenalty = 40;
                    singleReason = `Dangerous Whale (Single holder owns ${largestHolder.percent.toFixed(1)}%)`;
                } else if (largestHolder.percent > 30) {
                    singlePenalty = 30;
                    singleReason = `Whale risk (Single holder owns ${largestHolder.percent.toFixed(1)}%)`;
                } else if (largestHolder.percent > 20) {
                    singlePenalty = 20;
                    singleReason = `Whale risk (Single holder owns ${largestHolder.percent.toFixed(1)}%)`;
                } else if (largestHolder.percent > 10) {
                    singlePenalty = 10;
                    singleReason = `Minor Whale risk (Single holder owns ${largestHolder.percent.toFixed(1)}%)`;
                }
            }
        }
        // Apply the MAX of the two penalties
        if (singlePenalty > top10Penalty) {
            penalties.push({
                category: "Top Holder Risk",
                reason: `${singleReason} (Top 10: ${topTenPercent.toFixed(1)}%)`,
                points: singlePenalty
            });
        } else if (top10Penalty > 0) {
            penalties.push({
                category: "Top Holder Risk",
                reason: `${top10Reason} (Largest: ${largestHolder?.percent.toFixed(1)}%)`,
                points: top10Penalty
            });
        }
        // 6.5 Clusters (Identical or tight-range Balances)
        if (checks.holders.data.clustersDetected >= 3) {
            const clusterCount = checks.holders.data.clustersDetected;
            let points = 15;
            if (clusterCount >= 10) points = 50; // Critical for large clusters
            else if (clusterCount >= 6) points = 30;
            penalties.push({
                category: "Insider Activity",
                reason: `${clusterCount} wallets have highly suspicious similar balances (Bundled launch risk)`,
                points: points,
                isCritical: points >= 50
            });
        }
    }
    // -- Low Risk / Other --
    // Metadata
    if (mode !== "pump" && !skip.metadata) {
        if (checks.metadata.data?.isMutable) {
            penalties.push({
                category: "Mutable Metadata",
                reason: "Metadata is mutable",
                points: 5
            });
        }
        const hasSocials = checks.metadata.data?.website || checks.metadata.data?.twitter || checks.metadata.data?.telegram;
        if (!hasSocials) {
            penalties.push({
                category: "No Socials",
                reason: "No social links found",
                points: 10
            });
        }
    }
    // Age (Skip for Pump)
    if (mode !== "pump" && !skip.age && checks.tokenAge.data) {
        if (checks.tokenAge.data.ageInHours < 24) {
            penalties.push({
                category: "New Token",
                reason: "Token is very new (< 24h)",
                points: 15
            });
        }
    }
    // Final Calculation
    const totalPoints = penalties.reduce((sum, p)=>sum + p.points, 0);
    const finalScore = Math.max(0, 100 - totalPoints);
    return {
        score: finalScore,
        riskFactors: penalties
    };
}
// --- Main Function ---
// Short-lived in-flight/result cache to prevent duplicate scans
// (layout generateMetadata + client POST + React Strict Mode)
const scanInflight = new Map();
const INFLIGHT_TTL_MS = 30_000; // 30 seconds
async function scanToken(tokenAddress) {
    const now = Date.now();
    const existing = scanInflight.get(tokenAddress);
    if (existing && now - existing.ts < INFLIGHT_TTL_MS) {
        return existing.promise;
    }
    const promise = _scanTokenImpl(tokenAddress);
    scanInflight.set(tokenAddress, {
        promise,
        ts: now
    });
    // Clean up after TTL
    promise.finally(()=>{
        setTimeout(()=>scanInflight.delete(tokenAddress), INFLIGHT_TTL_MS);
    });
    return promise;
}
async function _scanTokenImpl(tokenAddress) {
    // 1. Whitelist Check
    let whitelist = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$whitelist$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getWhitelistedToken"])(tokenAddress);
    let jupiterVerified = false;
    if (!whitelist) {
        const jupInfo = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$jupiter$2d$tokens$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getJupiterTokenInfo"])(tokenAddress);
        if (jupInfo) {
            jupiterVerified = true;
            whitelist = {
                address: tokenAddress,
                name: jupInfo.name,
                symbol: jupInfo.symbol,
                type: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$jupiter$2d$tokens$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getTokenType"])(jupInfo),
                skipChecks: {
                    age: true
                },
                minScore: 60
            };
        }
    }
    // 2. Fetch Initial Data
    const WSOL_ADDRESS = "So11111111111111111111111111111111111111112";
    const pumpStatePromise = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$pumpfun$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"])(tokenAddress);
    const pricePromise = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$price$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getTokenPrice"])(tokenAddress).catch((e)=>{
        console.warn("Price fetch failed:", e);
        return null;
    });
    const solPricePromise = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$price$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getTokenPrice"])(WSOL_ADDRESS);
    const contextPromise = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$connection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchTokenContext"])(tokenAddress);
    const [pumpState, context, solPriceData] = await Promise.all([
        pumpStatePromise,
        contextPromise,
        solPricePromise
    ]);
    // 3. Determine Mode
    const { mode, dexData } = await detectTokenMode(tokenAddress, pumpState, context, !!whitelist);
    // 4. Run Checks
    const [mintAuth, freezeAuth, holders, liquidity, metadata, tokenAge, price, honeypot] = await Promise.all([
        safeCheck(()=>context ? Promise.resolve((0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$authority$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkMintAuthority"])(context)) : Promise.resolve(null), "mintAuthority"),
        safeCheck(()=>context ? Promise.resolve((0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$authority$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkFreezeAuthority"])(context)) : Promise.resolve(null), "freezeAuthority"),
        safeCheck(async ()=>{
            if (!context) return null;
            // Exclude bonding curve PDA from holders analysis
            const excludes = pumpState?.exists ? [
                pumpState.pda
            ] : [];
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$holders$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["analyzeHolders"])(tokenAddress, context.supply, excludes);
        }, "holders"),
        safeCheck(async ()=>{
            if (mode === "pump" && pumpState) {
                return {
                    lpSizeUsd: 0,
                    lpLocked: true,
                    lpBurned: true,
                    lockDuration: "Bonding Curve",
                    dexName: "pump.fun",
                    pairAddress: null,
                    liquidityDisplay: `Bonding Curve (${pumpState.curveProgressPercent}% 🔥)`
                };
            }
            // Pass prefetched dexData if available
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$liquidity$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkLiquidity"])(tokenAddress, dexData);
        }, "liquidity"),
        safeCheck(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$metadata$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getTokenMetadata"])(tokenAddress), "metadata"),
        safeCheck(async ()=>{
            const p = await pricePromise;
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$solana$2f$tokenAge$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkTokenAge"])(tokenAddress, p?.pairCreatedAt);
        }, "tokenAge"),
        safeCheck(()=>pricePromise, "price"),
        safeCheck(()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$external$2f$jupiter$2d$swap$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkSellSimulation"])(tokenAddress), "honeypot")
    ]);
    // Fallback: If mode is Pump but we failed to fetch pumpState (RPC error or bad ID),
    // construct a partial state using Market Cap from price API and dynamic SOL price.
    let finalPumpState = pumpState;
    if (mode === 'pump' && (!finalPumpState || !finalPumpState.exists)) {
        const currentMc = price.data?.marketCap || 0;
        const solPrice = solPriceData?.priceUsd || 600;
        // Pump.fun Graduation Math:
        // - Real SOL collected at graduation: 85 SOL
        // - Virtual SOL at graduation: ~30-40 SOL (initial virtual liquidity)
        // - Virtual tokens remaining: ~280M (after selling 793M of 1.073B)
        // - Market Cap = (virtualSol / virtualTokens) × totalSupply
        // - At graduation: MC ≈ (35 / (280M / 1e6)) × 1B ≈ 125 SOL
        // - In USD: 125 SOL × $600 ≈ $75k (varies with SOL price)
        const PUMP_GRADUATION_MC_SOL = 115; // Empirical MC at graduation (in SOL)
        const PUMP_GRADUATION_REAL_SOL = 85; // Real SOL collected at graduation
        const graduationTargetUsd = solPrice * PUMP_GRADUATION_MC_SOL;
        const currentMcInSol = currentMc / solPrice;
        const fallbackProgress = Math.min(100, Math.round(currentMc / graduationTargetUsd * 100));
        // Estimate remaining SOL using the ratio: realSOL ≈ (MC_SOL / 115) × 85
        const estimatedRealSol = currentMcInSol / PUMP_GRADUATION_MC_SOL * PUMP_GRADUATION_REAL_SOL;
        const remainingSolEstimate = Math.max(0, PUMP_GRADUATION_REAL_SOL - estimatedRealSol);
        finalPumpState = {
            exists: true,
            pda: "Estimated from MC",
            virtualTokenReserves: "0",
            virtualSolReservesLamports: "0",
            virtualSolReservesSol: 0,
            realTokenReserves: "0",
            realSolReservesLamports: "0",
            realSolReservesSol: estimatedRealSol,
            tokenTotalSupply: 1_000_000_000,
            complete: fallbackProgress >= 100,
            pricePerTokenSol: null,
            marketCapSol: currentMcInSol,
            curveProgressPercent: fallbackProgress,
            remainingSolToGraduate: fallbackProgress >= 100 ? 0 : remainingSolEstimate
        };
    }
    // 5. Advanced Analysis
    const advancedData = analyzeDevWallet(metadata.data, holders.data);
    const checks = {
        mintAuthority: mintAuth,
        freezeAuthority: freezeAuth,
        holders,
        liquidity,
        metadata,
        tokenAge,
        honeypot,
        advanced: {
            status: "success",
            data: advancedData
        }
    };
    // Attach derived metadata
    if (pumpState && checks.metadata.data) checks.metadata.data.pumpFunCurve = pumpState;
    // Apply Whitelist Overrides
    if (whitelist && checks.metadata.data) {
        if (!checks.metadata.data.name || checks.metadata.data.name === "Unknown") checks.metadata.data.name = whitelist.name;
        if (!checks.metadata.data.symbol || checks.metadata.data.symbol === "TOKEN") checks.metadata.data.symbol = whitelist.symbol;
        if (whitelist.image && !checks.metadata.data.image) checks.metadata.data.image = whitelist.image;
    }
    // 6. Calculate Score
    let { score, riskFactors } = calculatePenalties(checks, mode, whitelist);
    // Apply Whitelist Floor Score
    if (whitelist?.minScore && score < whitelist.minScore) {
        score = whitelist.minScore;
    }
    // 7. Final Grade (with Auto-F logic)
    // Whitelisted tokens with minScore bypass criticalFail — the whitelist is an explicit trust signal
    const hasCriticalFail = whitelist ? false : riskFactors.some((r)=>r.isCritical);
    let { grade, color, label } = getGrade(score, hasCriticalFail);
    // Override label for severe F grades
    if (grade === 'F') {
        const totalPenalty = riskFactors.reduce((sum, p)=>sum + p.points, 0);
        if (totalPenalty > 170) label = "High Confidence Scam";
        else if (totalPenalty > 130) label = "Extreme Risk";
    // else default "Likely Scam"
    }
    return {
        tokenAddress,
        scanMode: mode,
        score,
        grade,
        gradeColor: color,
        gradeLabel: label,
        penalties: riskFactors,
        totalPenalties: riskFactors.reduce((sum, p)=>sum + p.points, 0),
        checks,
        scannedAt: new Date(),
        isWhitelisted: !!whitelist,
        whitelistInfo: whitelist ? {
            name: whitelist.name,
            symbol: whitelist.symbol,
            type: jupiterVerified ? `jupiter-verified (${whitelist.type})` : whitelist.type
        } : undefined,
        price: price.data,
        solPrice: solPriceData?.priceUsd || null,
        bondingCurveData: finalPumpState
    };
}
;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[project]/lib/storage/recent-scans.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addRecentScan",
    ()=>addRecentScan,
    "getRecentScanByAddress",
    ()=>getRecentScanByAddress,
    "getRecentScans",
    ()=>getRecentScans,
    "getRecentScansCount",
    ()=>getRecentScansCount,
    "getStats",
    ()=>getStats
]);
/**
 * Persistent storage for recent scans & stats.
 * Data is kept in memory for speed and flushed to a JSON file on disk
 * so it survives server restarts.
 */ var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
;
;
// --- File path for persistence ---
const DATA_DIR = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), "data");
const DATA_FILE = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(DATA_DIR, "scans.json");
function loadFromDisk() {
    try {
        if (__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(DATA_FILE)) {
            const raw = __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].readFileSync(DATA_FILE, "utf-8");
            return JSON.parse(raw);
        }
    } catch (e) {
        console.warn("Failed to load scans data from disk:", e);
    }
    return null;
}
let saveTimeout = null;
function saveToDisk() {
    // Debounce: wait 2s after last write to batch multiple rapid scans
    if (saveTimeout) clearTimeout(saveTimeout);
    saveTimeout = setTimeout(()=>{
        try {
            if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(DATA_DIR)) {
                __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(DATA_DIR, {
                    recursive: true
                });
            }
            const data = {
                recentScans: globalRecentScans.recentScans,
                totalScans: globalRecentScans.totalScans,
                rugsDetected: globalRecentScans.rugsDetected,
                scannedAddresses: Array.from(globalRecentScans.scannedAddresses)
            };
            __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(DATA_FILE, JSON.stringify(data), "utf-8");
        } catch (e) {
            console.warn("Failed to save scans data to disk:", e);
        }
    }, 2000);
}
// --- In-memory state (globalThis for HMR in dev) ---
const globalRecentScans = globalThis;
if (!globalRecentScans._loaded) {
    const saved = loadFromDisk();
    if (saved) {
        globalRecentScans.recentScans = saved.recentScans;
        globalRecentScans.totalScans = saved.totalScans;
        globalRecentScans.rugsDetected = saved.rugsDetected;
        globalRecentScans.scannedAddresses = new Set(saved.scannedAddresses);
        console.log(`Loaded ${saved.totalScans} scans, ${saved.recentScans.length} recent from disk`);
    } else {
        globalRecentScans.recentScans = [];
        globalRecentScans.totalScans = 0;
        globalRecentScans.rugsDetected = 0;
        globalRecentScans.scannedAddresses = new Set();
    }
    globalRecentScans._loaded = true;
}
// Store last 50 scans
const MAX_RECENT_SCANS = 50;
const recentScans = globalRecentScans.recentScans;
// Stats
const scannedAddresses = globalRecentScans.scannedAddresses;
function addRecentScan(scan) {
    // Update stats only for new tokens
    if (!scannedAddresses.has(scan.address)) {
        scannedAddresses.add(scan.address);
        globalRecentScans.totalScans++;
        if (scan.score < 40) {
            globalRecentScans.rugsDetected++;
        }
    }
    // Check if this token was already scanned recently
    const existingIndex = recentScans.findIndex((s)=>s.address === scan.address);
    if (existingIndex !== -1) {
        // Update existing entry and move to front
        recentScans.splice(existingIndex, 1);
    }
    // Add to beginning
    recentScans.unshift(scan);
    // Keep only last MAX_RECENT_SCANS
    if (recentScans.length > MAX_RECENT_SCANS) {
        recentScans.pop();
    }
    // Persist to disk
    saveToDisk();
}
function getRecentScanByAddress(address) {
    return recentScans.find((s)=>s.address === address) || null;
}
function getRecentScans(limit = 12) {
    return recentScans.slice(0, limit);
}
function getRecentScansCount() {
    return recentScans.length;
}
function getStats() {
    return {
        totalScans: globalRecentScans.totalScans,
        rugsDetected: globalRecentScans.rugsDetected
    };
}
}),
"[project]/lib/utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "cn",
    ()=>cn,
    "isValidSolanaAddress",
    ()=>isValidSolanaAddress
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$clsx$2f$dist$2f$clsx$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/clsx/dist/clsx.mjs [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tailwind$2d$merge$2f$dist$2f$bundle$2d$mjs$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tailwind-merge/dist/bundle-mjs.mjs [app-route] (ecmascript)");
;
;
function cn(...inputs) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tailwind$2d$merge$2f$dist$2f$bundle$2d$mjs$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["twMerge"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$clsx$2f$dist$2f$clsx$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["clsx"])(inputs));
}
function isValidSolanaAddress(address) {
    if (!address) return false;
    // Basic length check for base58 string
    if (address.length < 32 || address.length > 44) return false;
    // Regex for base58 characters
    return /^[1-9A-HJ-NP-Za-km-z]+$/.test(address);
}
}),
"[project]/lib/utils/og.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildOgQuery",
    ()=>buildOgQuery
]);
function formatCompact(num) {
    if (num >= 1_000_000_000) return `$${(num / 1_000_000_000).toFixed(1)}B`;
    if (num >= 1_000_000) return `$${(num / 1_000_000).toFixed(1)}M`;
    if (num >= 1_000) return `$${(num / 1_000).toFixed(1)}K`;
    return `$${num.toFixed(0)}`;
}
function buildOgQuery(result) {
    const meta = result.checks.metadata.data;
    const price = result.price;
    const liq = result.checks.liquidity.data;
    const h = result.checks.holders.data;
    const adv = result.checks.advanced?.data;
    const topPenalties = result.penalties.slice(0, 3);
    return new URLSearchParams({
        address: result.tokenAddress,
        name: meta?.name || "Unknown",
        symbol: meta?.symbol || "TOKEN",
        score: result.score.toString(),
        grade: result.grade,
        label: result.gradeLabel,
        mode: result.scanMode,
        price: price?.priceUsd ? `$${parseFloat(price.priceUsd < 0.0001 ? price.priceUsd.toFixed(8) : price.priceUsd.toFixed(4))}` : "$0.00",
        mcap: price?.marketCap ? formatCompact(price.marketCap) : "0",
        change: price?.priceChange?.h24 ? `${price.priceChange.h24 > 0 ? "+" : ""}${price.priceChange.h24.toFixed(1)}%` : "0%",
        liq: result.scanMode === "pump" ? `${result.bondingCurveData?.curveProgressPercent || 0}%` : liq?.lpSizeUsd ? formatCompact(liq.lpSizeUsd) : "$0",
        top10: h ? `${h.topTenPercent.toFixed(1)}%` : "0%",
        lock: result.scanMode === "pump" ? adv?.sniperCount?.toString() || "0" : liq?.lpBurned ? "Burned" : "No",
        sell: result.checks.honeypot.data?.isHoneypot ? "No" : "Yes",
        mint: result.checks.mintAuthority.data?.status === "pass" ? "Revoked" : "Active",
        penalty: result.totalPenalties.toString(),
        tags: topPenalties.map((p)=>p.category).join(",") || "Safe,Verified,Low Risk",
        tagPoints: topPenalties.map((p)=>p.points).join(","),
        image: meta?.image || ""
    }).toString();
}
}),
"[project]/lib/auth/rate-limit.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "checkScanLimit",
    ()=>checkScanLimit,
    "createFingerprint",
    ()=>createFingerprint,
    "getWalletForFingerprint",
    ()=>getWalletForFingerprint,
    "linkWalletToFingerprint",
    ()=>linkWalletToFingerprint,
    "recordScan",
    ()=>recordScan
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/crypto [external] (crypto, cjs)");
;
;
;
// Путь к файлу хранения использованных сканов
const DATA_DIR = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), "data");
const USAGE_FILE = __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(DATA_DIR, "scan-usage.json");
// In-memory кеш для быстрого доступа
let usageCache = null;
/**
 * Создаёт директорию data/ если её нет
 */ function ensureDataDir() {
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(DATA_DIR)) {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].mkdirSync(DATA_DIR, {
            recursive: true
        });
    }
}
/**
 * Загружает данные использования из файла
 */ function loadUsageData() {
    if (usageCache) return usageCache;
    ensureDataDir();
    if (!__TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(USAGE_FILE)) {
        usageCache = {
            fingerprints: new Map(),
            wallets: new Map()
        };
        return usageCache;
    }
    try {
        const raw = __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].readFileSync(USAGE_FILE, "utf-8");
        const parsed = JSON.parse(raw);
        usageCache = {
            fingerprints: new Map(Object.entries(parsed.fingerprints || {})),
            wallets: new Map(Object.entries(parsed.wallets || {}))
        };
        return usageCache;
    } catch (error) {
        console.error("Failed to load usage data:", error);
        usageCache = {
            fingerprints: new Map(),
            wallets: new Map()
        };
        return usageCache;
    }
}
/**
 * Сохраняет данные использования в файл
 */ function saveUsageData(data) {
    ensureDataDir();
    const toSave = {
        fingerprints: Object.fromEntries(data.fingerprints),
        wallets: Object.fromEntries(data.wallets)
    };
    try {
        __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].writeFileSync(USAGE_FILE, JSON.stringify(toSave, null, 2), "utf-8");
        usageCache = data;
    } catch (error) {
        console.error("Failed to save usage data:", error);
    }
}
function createFingerprint(ip, userAgent) {
    const combined = `${ip}|${userAgent}`;
    return __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__["default"].createHash("sha256").update(combined).digest("hex");
}
function checkScanLimit(fingerprint, walletAddress) {
    const data = loadUsageData();
    // Если есть verified wallet — проверяем лимиты кошелька
    if (walletAddress) {
        const walletData = data.wallets.get(walletAddress);
        // Для authenticated пользователей — безлимит
        return {
            allowed: true,
            remaining: Infinity,
            needsAuth: false
        };
    }
    // Для неавторизованных — только 1 скан
    const fingerprintData = data.fingerprints.get(fingerprint);
    if (!fingerprintData) {
        // Первый скан — разрешаем
        return {
            allowed: true,
            remaining: 0,
            needsAuth: false
        };
    }
    // Уже использовали бесплатный скан
    return {
        allowed: false,
        remaining: 0,
        needsAuth: true
    };
}
function recordScan(fingerprint, walletAddress) {
    const data = loadUsageData();
    if (walletAddress) {
        // Записываем скан для authenticated пользователя
        const walletData = data.wallets.get(walletAddress) || {
            scans: 0,
            lastScan: Date.now()
        };
        walletData.scans++;
        walletData.lastScan = Date.now();
        data.wallets.set(walletAddress, walletData);
        // Также связываем fingerprint с кошельком
        const fingerprintData = data.fingerprints.get(fingerprint) || {
            count: 0,
            lastReset: Date.now()
        };
        fingerprintData.walletAddress = walletAddress;
        data.fingerprints.set(fingerprint, fingerprintData);
    } else {
        // Записываем скан для неавторизованного
        const fingerprintData = data.fingerprints.get(fingerprint) || {
            count: 0,
            lastReset: Date.now()
        };
        fingerprintData.count++;
        data.fingerprints.set(fingerprint, fingerprintData);
    }
    saveUsageData(data);
}
function linkWalletToFingerprint(fingerprint, walletAddress) {
    const data = loadUsageData();
    const fingerprintData = data.fingerprints.get(fingerprint) || {
        count: 0,
        lastReset: Date.now()
    };
    fingerprintData.walletAddress = walletAddress;
    data.fingerprints.set(fingerprint, fingerprintData);
    saveUsageData(data);
}
function getWalletForFingerprint(fingerprint) {
    const data = loadUsageData();
    const fingerprintData = data.fingerprints.get(fingerprint);
    return fingerprintData?.walletAddress || null;
}
}),
"[project]/lib/auth/wallet.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "generateNonce",
    ()=>generateNonce,
    "isDevEnvironment",
    ()=>isDevEnvironment,
    "isOwnerWallet",
    ()=>isOwnerWallet,
    "verifyWalletSignature",
    ()=>verifyWalletSignature
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@solana/web3.js/lib/index.esm.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tweetnacl$2f$nacl$2d$fast$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/tweetnacl/nacl-fast.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bs58$2f$src$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/bs58/src/esm/index.js [app-route] (ecmascript)");
;
;
;
function verifyWalletSignature(walletAddress, signature, message) {
    try {
        const publicKey = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$solana$2f$web3$2e$js$2f$lib$2f$index$2e$esm$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PublicKey"](walletAddress);
        const messageBytes = new TextEncoder().encode(message);
        const signatureBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bs58$2f$src$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].decode(signature);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tweetnacl$2f$nacl$2d$fast$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].sign.detached.verify(messageBytes, signatureBytes, publicKey.toBytes());
    } catch (error) {
        console.error("Wallet signature verification error:", error);
        return false;
    }
}
function generateNonce() {
    const timestamp = Date.now();
    const random = Math.random().toString(36).substring(2, 15);
    return `rug-scanner-auth-${timestamp}-${random}`;
}
function isOwnerWallet(walletAddress) {
    const ownerWallets = (process.env.OWNER_WALLETS || "").split(",").map((w)=>w.trim());
    return ownerWallets.includes(walletAddress);
}
function isDevEnvironment() {
    return ("TURBOPACK compile-time value", "development") === "development";
}
}),
"[project]/app/api/scan/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET,
    "POST",
    ()=>POST
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/lib/scoring/engine.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$storage$2f$recent$2d$scans$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/storage/recent-scans.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/utils.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2f$og$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/utils/og.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$rate$2d$limit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/auth/rate-limit.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$wallet$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/auth/wallet.ts [app-route] (ecmascript)");
;
;
;
;
;
;
;
// In-memory cache (replace with Redis in production)
const scanCache = new Map();
const CACHE_TTL_MS = 5 * 60 * 1000; // 5 minutes
function getClientIp(request) {
    const forwarded = request.headers.get("x-forwarded-for");
    if (forwarded) {
        return forwarded.split(",")[0].trim();
    }
    const realIp = request.headers.get("x-real-ip");
    if (realIp) {
        return realIp;
    }
    return "unknown";
}
function getCachedResult(address) {
    const cached = scanCache.get(address);
    if (!cached) return null;
    const now = Date.now();
    if (now - cached.timestamp > CACHE_TTL_MS) {
        scanCache.delete(address);
        return null;
    }
    return cached.result;
}
function setCachedResult(address, result) {
    scanCache.set(address, {
        result,
        timestamp: Date.now()
    });
    // Clean up old cache entries periodically
    if (scanCache.size > 1000) {
        const now = Date.now();
        for (const [key, value] of scanCache.entries()){
            if (now - value.timestamp > CACHE_TTL_MS) {
                scanCache.delete(key);
            }
        }
    }
}
async function POST(request) {
    try {
        // Создаём fingerprint пользователя
        const clientIp = getClientIp(request);
        const userAgent = request.headers.get("user-agent") || "";
        const fingerprint = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$rate$2d$limit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createFingerprint"])(clientIp, userAgent);
        // Проверяем, есть ли привязанный wallet
        const walletAddress = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$rate$2d$limit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getWalletForFingerprint"])(fingerprint);
        // Проверяем, является ли пользователь владельцем (безлимит)
        const isOwner = walletAddress ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$wallet$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isOwnerWallet"])(walletAddress) : false;
        // В dev режиме — автоматический безлимит
        const isDev = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$wallet$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isDevEnvironment"])();
        // Сканирование требует авторизации. Владельцы и dev режим — исключение.
        if (!isOwner && !walletAddress && !isDev) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Authentication required",
                message: "Connect your wallet to scan tokens.",
                needsAuth: true
            }, {
                status: 429,
                headers: {
                    "X-Needs-Auth": "true"
                }
            });
        }
        // Parse body
        let body;
        try {
            body = await request.json();
        } catch  {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Invalid JSON body"
            }, {
                status: 400
            });
        }
        const { address } = body;
        // Validate address
        if (!address || typeof address !== "string") {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Missing or invalid 'address' field"
            }, {
                status: 400
            });
        }
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isValidSolanaAddress"])(address)) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Invalid Solana address"
            }, {
                status: 400
            });
        }
        // Run scan
        const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$scoring$2f$engine$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["scanToken"])(address);
        // Cache result
        setCachedResult(address, result);
        // Записываем использование скана
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$auth$2f$rate$2d$limit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["recordScan"])(fingerprint, walletAddress || undefined);
        // Add to recent scans with OG query for Twitter cards
        const metadata = result.checks.metadata.data;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$storage$2f$recent$2d$scans$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addRecentScan"])({
            address: result.tokenAddress,
            name: metadata?.name || result.whitelistInfo?.name || "Unknown",
            symbol: metadata?.symbol || result.whitelistInfo?.symbol || "???",
            image: metadata?.image || undefined,
            score: result.score,
            grade: result.grade,
            gradeLabel: result.gradeLabel,
            gradeColor: result.gradeColor,
            scannedAt: result.scannedAt.toISOString(),
            createdAt: metadata?.createdAt || undefined,
            ogQuery: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2f$og$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["buildOgQuery"])(result)
        });
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            success: true,
            cached: false,
            data: result
        }, {
            headers: {
                "X-Is-Authenticated": String(!!walletAddress),
                "X-Is-Owner": String(isOwner || isDev)
            }
        });
    } catch (error) {
        console.error("Scan API error:", error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Internal server error",
            message: error instanceof Error ? error.message : "Unknown error"
        }, {
            status: 500
        });
    }
}
async function GET(request) {
    try {
        const url = new URL(request.url);
        const address = url.searchParams.get("address");
        // If an address query param is provided, try to return a cached scan
        if (address) {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isValidSolanaAddress"])(address)) {
                return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                    error: "Invalid Solana address"
                }, {
                    status: 400
                });
            }
            const cachedResult = getCachedResult(address);
            if (cachedResult) {
                // Bump in recent scans even if cached
                const metadata = cachedResult.checks.metadata.data;
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$storage$2f$recent$2d$scans$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addRecentScan"])({
                    address: cachedResult.tokenAddress,
                    name: metadata?.name || cachedResult.whitelistInfo?.name || "Unknown",
                    symbol: metadata?.symbol || cachedResult.whitelistInfo?.symbol || "???",
                    image: metadata?.image || undefined,
                    score: cachedResult.score,
                    grade: cachedResult.grade,
                    gradeLabel: cachedResult.gradeLabel,
                    gradeColor: cachedResult.gradeColor,
                    scannedAt: new Date().toISOString(),
                    createdAt: metadata?.createdAt || undefined,
                    ogQuery: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$utils$2f$og$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["buildOgQuery"])(cachedResult)
                });
                return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                    success: true,
                    cached: true,
                    data: cachedResult
                }, {
                    headers: {
                        "X-Cache": "HIT"
                    }
                });
            }
            // No cached result — return 404 (не тратим скан!)
            // Клиент должен явно запустить POST запрос для нового скана
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                success: false,
                cached: false,
                notFound: true,
                message: "No cached scan found. Click 'Scan Token' to analyze."
            }, {
                status: 404,
                headers: {
                    "X-Cache": "MISS"
                }
            });
        }
        // Default health response (no address provided)
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            status: "ok",
            cacheSize: scanCache.size
        });
    } catch (error) {
        console.error("Scan GET error:", error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Internal error"
        }, {
            status: 500
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__7480d799._.js.map